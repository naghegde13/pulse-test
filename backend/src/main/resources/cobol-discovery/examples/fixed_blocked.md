# Fixed Blocked (FB)

Use when records are grouped into fixed-size blocks.

Key options:
- `record_format=FB`
- `record_length`
- `records_per_block` or `block_length`

Typical clue:
- Fixed-length parsing works only when records are evaluated inside block boundaries.

# Record Length Map

Use when a controlling field maps to multiple fixed record sizes.

Key options:
- `record_length_field`
- `record_length_map`

Typical clue:
- Different record types have different total lengths without RDW headers.

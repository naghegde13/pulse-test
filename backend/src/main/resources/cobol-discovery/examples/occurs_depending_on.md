# OCCURS Depending On

Use when array cardinality is controlled by another field in the record.

Key options:
- `variable_size_occurs=true`
- consider `occurs_mapping` when the controlling field is not numeric

Typical clue:
- Preview columns appear shifted after the first repeated group.

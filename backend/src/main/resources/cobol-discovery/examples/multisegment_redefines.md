# Multisegment Redefines

Use when the copybook models multiple record shapes via `REDEFINES`.

Key options:
- `segment_field`
- `redefine_segment_id_map`
- optionally `segment-children`

Typical clue:
- Some rows parse correctly while others decode into the wrong redefine branch.

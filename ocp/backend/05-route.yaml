apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pulse-backend
  labels:
    app: pulse-backend
spec:
  to:
    kind: Service
    name: pulse-backend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

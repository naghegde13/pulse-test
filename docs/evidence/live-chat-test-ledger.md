# Live Chat Test Ledger

Date started: 2026-06-17
Branch: guided/acme-lending-human-rehearsal-2026-05-25
Purpose: Track issues, observations, and verification evidence while testing the PULSE chat/build delivery locally.

## Repo Baseline

| Time | Fact | Evidence |
|---|---|---|
| 2026-06-17 | Local branch fast-forward checked against origin; already up to date at `e54751cff1e8a82327fd7549b7f1ea6d5e658a92`. | `git fetch origin`; `git pull --ff-only origin guided/acme-lending-human-rehearsal-2026-05-25`; `git rev-parse HEAD origin/guided/acme-lending-human-rehearsal-2026-05-25` |
| 2026-06-17 18:14 ET | Fixed remediation build pulled and local servers restarted from new head `45a735997f3d4b5228ea0894c37932aac7952ab2` (`45a7359 Finish autonomous build remediation`). | `git fetch origin guided/acme-lending-human-rehearsal-2026-05-25`; `git pull --ff-only origin guided/acme-lending-human-rehearsal-2026-05-25`; backend restarted with `./gradlew bootRun --args='--pulse.chat.orchestration=graph'`; frontend restarted with `npm run dev`; backend `/actuator/health` returned `UP`; frontend `HEAD /settings` returned `200`. |

## Monitor Setup Notes

| Time | Note | Evidence |
|---|---|---|
| 2026-06-17 | Chat transcript source is Postgres table `chat_messages`, keyed by `session_id`; chat sessions live in `chat_sessions`. Fast monitor should poll this table directly at ~1 second cadence during testing. | `backend/src/main/resources/db/migration/V20__chat_sessions.sql`; `backend/src/main/java/com/pulse/chat/model/ChatMessage.java` |

## Issues

| ID | Time | Area | Symptom | Status | Evidence / Notes |
|---|---|---|---|---|---|
| LCT-012 | 2026-06-17 18:15 ET | Pipeline DAG default zoom | On Card Transaction Curation (`/pipelines/01JDEMO0UL0CARDTXN000001`), the default zoom level where the graphical DAG is shown is too large. | Open / UX tuning needed | User observed the issue immediately after opening the pipeline on fixed build `45a7359`. Likely implementation cause: full-screen/fill-parent `DagView` forcibly calls `instance.setViewport({ x: 80, y: 100, zoom: 2.0 })`, with comments saying this intentionally makes nodes larger (`frontend/src/components/pipeline/dag-view.tsx:329-340`). Candidate fix: lower the forced initial zoom and/or use a responsive `fitView`/bounds-aware initial viewport so 5-step seeded DAGs fit comfortably by default. |
| LCT-013 | 2026-06-17 18:17 ET | Pipeline outline-to-DAG navigation / inspector | Clicking a DAG node in the left outline should navigate/focus that node in the graphical DAG and open that node's configuration in the right configuration panel. | Open / behavior incomplete | User reported desired behavior on Card Transaction Curation (`/pipelines/01JDEMO0UL0CARDTXN000001`) on fixed build `45a7359`. Current code wires outline clicks to `setSelectedWorkspaceInstanceId` (`frontend/src/app/pipelines/[pipelineId]/page.tsx:734-739`, `1001-1005`) and opens `WorkspaceStepInspector` / closes chat when `selectedWorkspaceInstance` changes (`page.tsx:371-387`). However, no viewport/pan/zoom focus is wired from outline selection into `DagView`; `DagView` only marks the selected node (`frontend/src/components/pipeline/dag-view.tsx:232-240`). Also note canvas node click selection in `CompositionPanel` is gated by `editable` (`frontend/src/components/pipeline/composition-panel.tsx:209-216`), which may prevent inspector selection on non-ENGINEERING revisions. Acceptance: outline click should select the graph node, center/pan it into view at a comfortable zoom, and populate the right inspector config tab without requiring a separate canvas click. |
| LCT-014 | 2026-06-17 18:27 ET | Right configuration panel editability | The right-side Config panel opens for `Publish Spend Fact to Lake` but fields cannot be edited. | Open / inspector is read-only | User provided screenshot `CleanShot 2026-06-17 at 18.27.36@2x.png` showing `LakeWriter` config values rendered in the right panel with no inputs or save affordance. Code confirms `WorkspaceStepInspector` renders config as `InspectorRow` and param `<pre>` blocks (`frontend/src/app/pipelines/[pipelineId]/page.tsx:1088-1105`) and has no `PUT`/save path. The editable implementation still lives in `ConfigureTransformDialog`, which saves via `PUT /api/v1/versions/{versionId}/composition/instances/{instanceId}/params` (`frontend/src/components/pipeline/configure-transform-dialog.tsx:519-533`). Acceptance: the right config panel should provide editable controls using the same metadata-driven parameter UI/save behavior as the dialog, or expose a clear edit action that opens the editable form without losing selected node context. |
| LCT-015 | 2026-06-17 18:29 ET | Create Pipeline storage backend default | Create Pipeline fails with `defaultStorageBackend is required when creating a pipeline; must be 'DPC' or 'GCP'`, but this should be derived from the global PULSE runtime/environment mode. | Open / create flow omits required derived field | User provided screenshot `CleanShot 2026-06-17 at 18.29.05.png` from `/pipelines` showing the error. Backend log confirms `IllegalArgumentException: defaultStorageBackend is required when creating a pipeline; must be 'DPC' or 'GCP'`. Frontend `CreatePipelineDialog` posts only `name`, `description`, `domainName`, and `domainId` (`frontend/src/components/pipeline/create-pipeline-dialog.tsx:57-66`), while backend `PipelineService.create` rejects null/blank `defaultStorageBackend` (`backend/src/main/java/com/pulse/pipeline/service/PipelineService.java:96-107`). Product rule from user: PULSE is globally in either DPC or GCP mode, so by create time it already knows the storage backend and the user should not see this error. Acceptance: create-pipeline should populate `defaultStorageBackend` from global runtime authority/persona or backend should default it from the same source; no user-facing error for missing storage backend in normal create flow. Workaround for current local test: create via API with `defaultStorageBackend: "GCP"` and Unsecured Lending Domain 1 id `01KVBV6J6WVMMDGJZ5G5D50BRA`, then refresh `/pipelines`. |
| LCT-016 | 2026-06-17 18:35 ET | Producer credential dialog crash | Opening credential dialog on producer `01KVBVEAWY355TZEY19PN6V1HV` crashes the app with `Runtime TypeError: spec?.required?.includes is not a function`. | Open / frontend type guard missing | User provided screenshot `CleanShot 2026-06-17 at 18.35.00.png` from `/producers/01KVBVEAWY355TZEY19PN6V1HV`. Next.js overlay points to `frontend/src/components/sor/credential-dialog.tsx:295:55` in `isRequired`: `spec?.required?.includes(key) ?? false`. This assumes `spec.required` is always an array, but the loaded credential spec can provide a non-array value. Stack includes `renderField`, `CredentialDialog`, and `SORDDetailPage` (`frontend/src/app/producers/[sorId]/page.tsx:827-834`). Acceptance: normalize/guard `required` with `Array.isArray(spec?.required)` before calling `includes`, and ideally validate connector credential spec shape at load time so malformed specs render a helpful message instead of a runtime crash. |
| LCT-017 | 2026-06-17 18:37 ET | Define Dataset sample-file upload discoverability | On the first Define Dataset method screen, there is no explicit option to upload a sample file. | Open / UX affordance hidden or mislabeled | User provided screenshot `CleanShot 2026-06-17 at 18.36.21.png` from `/producers/01KVBVEAWY355TZEY19PN6V1HV`. Visible choices are `Infer from File Path`, `Upload Schema`, and `Manual Field Definition`; user expects a first-class `Upload Sample File` option. Code has an upload button only after selecting/continuing into `FILE_INFERENCE` (`frontend/src/components/sor/define-dataset-dialog.tsx:411-441`) and a separate CSV sample path inside `SCHEMA_UPLOAD` after changing `schemaFormat` to `CSV_SAMPLE` (`define-dataset-dialog.tsx:334-389`). Backend method list for file connectors returns only `FILE_INFERENCE`, `SCHEMA_UPLOAD`, and `MANUAL_DEFINITION` (`backend/src/main/java/com/pulse/sor/controller/DatasetDefinitionMethods.java:52-57`), while sample discovery API exists at `POST /api/v1/connector-instances/{ciId}/schema-discovery/sample` (`SchemaDiscoveryController.java:86-94`). Acceptance: expose `Upload Sample File` as an obvious method on the first screen, or rename/reword `Infer from File Path` to make clear it supports local sample upload on the next step. |
| LCT-018 | 2026-06-17 18:41 ET | Sample upload does not preview/infer schema/PII | After uploading sample file `loan_master.csv`, UI did not show file preview, did not call LLM/PII classification, and saved dataset now displays `No schema defined`. | Open / sample content discarded; no inference/classification pipeline | User provided screenshot `CleanShot 2026-06-17 at 18.40.43.png` from `/producers/01KVBVEAWY355TZEY19PN6V1HV`. DB row confirms dataset `Loan Master` (`01KVBVTNG3QZNGX0F5Z881VVTM`) has `definition_type=FILE_INFERENCE`, `definition_config={"glob":"*","path":"loan_master.csv","format":"csv"}`, `schema_snapshot=NULL`, `status=DRAFT`, `classification=INTERNAL`, and no `discovery_method`/`discovery_proof`. Frontend `FILE_INFERENCE` upload handler only records `file.name` and inferred extension (`define-dataset-dialog.tsx:422-436`); it does not read sample contents, does not show preview, and submit sends only `definitionConfig` (`define-dataset-dialog.tsx:159-161`). Backend saves `DRAFT` when `schemaSnapshot` is absent (`DatasetController.java:146-150`). Acceptance: local sample upload should read file content, show preview, call deterministic schema discovery and PII/classification path before save, persist `schemaSnapshot` + `discovery_proof`, and save as `SCHEMA_DEFINED` or clearly block save until inference completes. |
| LCT-019 | 2026-06-17 18:45 ET | Manual dataset save duplicate crash | Trying to manually create a dataset after the failed sample-upload dataset exists crashes with a generic runtime error. | Open / duplicate-name validation missing; frontend error handling too harsh | User provided screenshot `CleanShot 2026-06-17 at 18.45.17.png`. Frontend overlay points to `src/lib/api.ts:160` throwing from `ApiClient.executeRequest`, called by `handleSave` in `define-dataset-dialog.tsx:206`. Backend log shows the real cause: Postgres unique constraint `datasets_qualified_name_key` rejected duplicate `qualified_name=tenant-unsecured-lending.domain-1.raw.Loan Master`. Existing row is dataset `01KVBVTNG3QZNGX0F5Z881VVTM`, created by the earlier sample-upload attempt. Acceptance: dataset create should pre-check duplicate qualified names and return a 409/validation message such as "Dataset Loan Master already exists; edit existing dataset or choose another name"; frontend should render that inline rather than a runtime overlay. Recovery applied for testing: patched existing `Loan Master` to `SCHEMA_DEFINED`, `CONFIDENTIAL`, 21 schema fields, discovery method `manual_test_recovery`. Follow-up bug: API responses still serialize `schemaSnapshot.fields` as `{empty:false,traversableAgain:true}` even though DB JSONB contains a real array. |
| LCT-020 | 2026-06-17 18:49 ET | Schema fields hidden despite `SCHEMA_DEFINED` | Dataset shows `SCHEMA_DEFINED`, but expanding it does not show fields. | Open / temporary local hot patch applied for test unblock | User clarified that `Loan Master` now shows `SCHEMA_DEFINED` but no fields are visible. DB verification proves the row is correct: `schema_snapshot->'fields'` is a JSON array with 21 entries and first field `{"name":"loan_id","type":"VARCHAR","nullable":false,"pii":false}`. API response was wrong before workaround: `schemaSnapshot.fields` serialized as `{"empty":false,"traversableAgain":true}`. Producer UI expects `ds.schemaSnapshot.fields` to be an array and renders fields only when `schemaFields.length > 0` (`frontend/src/app/producers/[sorId]/page.tsx:447-518`, `729-797`). Temporary local workaround: `DatasetController` now re-reads `schema_snapshot::text` with `JdbcTemplate` and parses it through Jackson before returning dataset responses. Verified after backend restart: `/actuator/health=UP`; `GET /api/v1/datasets/01KVBVTNG3QZNGX0F5Z881VVTM` returns `status=SCHEMA_DEFINED`, `classification=CONFIDENTIAL`, `fields_type=list`, `fields_count=21`, first field `loan_id`. Durable fix still needed: replace this controller-level normalization with a DTO/entity JSON mapping fix and regression tests. |
| LCT-021 | 2026-06-17 18:55 ET | Pipeline page crash after adding canvas step | After adding a pipeline step to newly created pipeline `01KVBVCFXGPB1T5TQGWTDR826E`, the pipeline page will not load; Next.js overlay shows `output.map is not a function`. | Open / temporary local hot patch applied for test unblock | User provided screenshot `CleanShot 2026-06-17 at 18.54.28.png` from `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`. Stack points to `computeSchemaDiff` (`frontend/src/lib/schema-diff.ts:23`) called by `DagView` (`frontend/src/components/pipeline/dag-view.tsx:186`). New instance exists: `01KVBWNWKKMPX2QA08R359DY4P`, `FileIngestion`. DB has valid `sub_pipeline_instances.output_schema` with `columns` JSON array of 8 audit columns, but composition API initially returned `outputSchema={"empty":false,"traversableAgain":true}`. Temporary local workaround: `CompositionController` now re-reads `output_schema::text` with `JdbcTemplate` and parses through Jackson before returning composition responses. Verified after DevTools restart: `/actuator/health=UP`; `GET /api/v1/versions/01KVBVCFXH8E3GBAWN6T1YJ8BX/composition` returns `outputSchema_type=dict`, `columns_type=list`, `columns_count=8`; pipeline route returns `HTTP/1.1 200 OK`. Durable fix still needed: same JSONB mapping/DTO correction as LCT-020, plus frontend `Array.isArray` guards around schema diff inputs. |
| LCT-022 | 2026-06-17 19:04 ET | FileIngestion dataset binding / schema propagation | Manually added `FileIngestion` step had no visible dataset link; after setting `dataset_ids`, API initially serialized `params.dataset_names` as `{empty:false,traversableAgain:true}` and output schema stayed audit-only. | Open / temporary local hot patch plus data patch applied for test unblock | User asked how to link the right-side FileIngestion step to the defined dataset. Current instance `01KVBWNWKKMPX2QA08R359DY4P` started with `params={}`, `input_datasets=[]`, `output_datasets=[]`. Intended UI path appears to be `Add Data Source`, which sets `params.dataset_ids`, `dataset_names`, and `qualified_names` (`frontend/src/components/pipeline/add-source-dialog.tsx:150-167`); the generic canvas-added step does not expose an obvious dataset-binding control in the right panel. Binding was applied via `PUT /composition/instances/{id}/params` to dataset `Loan Master` (`01KVBVTNG3QZNGX0F5Z881VVTM`). API then showed another JSONB serialization failure on `params.dataset_names`, so the local `CompositionController` hot patch was extended to normalize `params::text` as well as `output_schema::text`. Schema propagation did not replace audit-only output with source business fields, so the test node's `output_schema` was patched to `Loan Master` 21 fields + 8 audit fields. Verified API now returns `dataset_names_type=list ['Loan Master']`, `dataset_ids_type=list`, `columns_count=29`, first columns `loan_id, borrower_name, ssn, email, phone`. Durable fix: expose/edit dataset binding in the step inspector or steer users through Add Data Source; fix JSONB DTO mapping; ensure propagation derives ingestion output schema from bound dataset fields plus audit columns. |
| LCT-023 | 2026-06-17 19:05 ET | FileIngestion pattern kind dropdown empty / filename mnemonic assist missing | In the FileIngestion config UI, `pattern_kind` dropdown has zero values, and `filename_pattern` lacks UI assist for incorporating date mnemonics into `{date}` patterns. | Open / temporary blueprint metadata hot patch applied for dropdown only | User observed this in the right-side config UI on pipeline `01KVBVCFXGPB1T5TQGWTDR826E`. Root cause for empty dropdown: FileIngestion `params_schema` declares `pattern_kind` as `type:"enum"` with `default:"template"` but no `options`; generic enum renderer maps `definition.options ?? []`, so it renders no choices. Historical migrations V92/V93 had `options:["template","glob","regex"]`, but V153 overwrote FileIngestion schema without options. Local data hot patch added `options:["template","literal","glob","regex"]` for FileIngestion and FileArrivalSensor. Broader UX gap remains: `date_value` has `ui_construct:"date-mnemonic-picker"`, but `filename_pattern` is a plain string input; user needs assist to insert/use `{date}` and choose mnemonics/date formats while building filenames. Durable fix: restore enum options in migration/catalog, add a filename-pattern construct that previews examples and ties `{date}` to the date mnemonic picker/date format. |
| LCT-024 | 2026-06-17 19:05 ET | FileIngestion date format incorrectly read-only platform default | `Date Format` renders as `Derived` / `Read-only` with `yyyyMMdd`, but file naming conventions vary and users must be able to override it. | Open / temporary blueprint metadata hot patch applied | User provided screenshot `CleanShot 2026-06-17 at 19.05.12.png` showing `Date Format` as derived/read-only from platform default. Root cause: FileIngestion `date_format` is seeded with `tier:"derived"` and `derivedFrom:"platform_default"` in V153; the config UI marks any `tier === "derived" || "system-derived"` as read-only (`configure-transform-dialog.tsx:146-148`). Local data hot patch changed FileIngestion and FileArrivalSensor `date_format` to `tier:"user"`, removed `derivedFrom`, kept `default:"yyyyMMdd"`, and expanded description to say default can be overridden for `yyyy-MM-dd`, `MMddyyyy`, timestamps, or other Java date formats. Durable fix: update migration/catalog source so `date_format` is user-overridable wherever filename patterns are user-authored; keep defaults as suggestions, not authority. |
| LCT-025 | 2026-06-17 19:07 ET | FileIngestion Date Value control is unclear | User does not understand what the `Date Value` / `Mnemonic`, `ISO date`, `Free-text` section is trying to achieve. | Open / UX copy and grouping issue | The intended behavior is: when `pattern_kind=template` and `filename_pattern` contains `{date}` such as `loan_master_{date}.csv`, `date_value` chooses the runtime date to substitute into `{date}` (`RUN_DATE`, `PBD`, ISO literal, or custom mnemonic) and `date_format` controls how that date is formatted. The UI presents these as separate abstract fields, so the mental model is hidden. Durable fix: replace scattered `filename_pattern`, `pattern_kind`, `date_value`, and `date_format` controls with a grouped filename-pattern builder that explains "find files named ...", inserts `{date}`, lets user choose mnemonic/ISO/custom date, shows examples/previews, and validates the pattern. |
| LCT-026 | 2026-06-17 19:08 ET | FileIngestion partition_by incorrectly read-only | `partition_by` renders as derived/read-only, but users must be able to define partitioning for landed files. | Open / temporary blueprint metadata hot patch applied | User reported `partition_by` is read-only and should be configurable. Root cause mirrors LCT-024: FileIngestion `partition_by` was seeded as a derived pipeline-storage param, and the metadata renderer treats derived params as read-only. Product expectation: users can choose partitions based on file layout, query patterns, and date/as-of fields; defaults can be suggested but not enforced. Local DB hot patch changed FileIngestion `partition_by` to `tier:"user"` and removed `derivedFrom`; verification query shows `partition_by|user|derivedFrom=false|string[]`. Durable fix: update canonical blueprint catalog/migrations and ideally render `partition_by` with a multi-column picker against the output/source schema. |
| LCT-027 | 2026-06-17 19:09 ET | Derived fields say `Resolved by PULSE` without showing resolved value | Any UI label that says `Resolved by PULSE` should also show the actual value it resolved to. | Open / resolver visibility missing | User reported that when the UI shows `Resolved by PULSE`, it should show what value was resolved. Code confirms `formatParamValue` returns the literal text `Resolved by PULSE` for empty values (`frontend/src/components/pipeline/configure-transform-dialog.tsx:150-153`), and `DerivedParamInspector` only adds `Derived from ...` plus `Read-only` (`configure-transform-dialog.tsx:1098-1124`). There is no resolved-value API/DTO path in this panel. Expected behavior: derived/read-only rows should display concrete values and source, e.g. `GCP — from pipeline storage`, `bronze — default for FileIngestion`, `US-FED — from domain calendar`, or an explicit unresolved warning if derivation failed. Durable fix: expose resolved values from backend configuration resolution (`BlueprintInstanceConfigurationService`/composition response or a dedicated config-resolution endpoint) and render value + source + confidence, not only placeholder text. |
| LCT-028 | 2026-06-17 19:10 ET | Lake format defaults/options still show Delta instead of runtime storage convention | Lake format should not default to Delta. GCP should resolve to BigQuery-managed Iceberg tables; DPC should resolve to Hive-managed Parquet files on S3. | Open / temporary local instance + metadata patch applied | User clarified storage rule. Evidence of mismatch: `RuntimeAuthorityService` already maps GCP bronze/silver to `iceberg_bq_managed` and DPC bronze/silver/gold to `parquet`, but migrations V97/V153 still seed `lake_format.default="delta"` across many blueprints and frontend `legalLakeFormats` allows `delta` for both DPC and GCP non-gold. Current test instance was `storage_backend=GCP` with null lake layer/format; local patch set `lake_layer=bronze`, `lake_format=iceberg_bq_managed`, and changed FileIngestion local blueprint `lake_format.default` description for continued testing. Durable fix: centralize lake format resolution from runtime authority; remove Delta defaults/options from GCP/DPC mode unless explicitly supported; update catalog, frontend option matrix, seed data, and codegen assumptions. |
| LCT-029 | 2026-06-17 19:12 ET | Saving FileIngestion config drops hidden dataset/connector binding | After binding FileIngestion to `Loan Master`, subsequent config editing left the node with only filename params; dataset/connector params disappeared and output schema regressed to audit-only. | Open / rebound locally for testing | Verification before rebound showed instance params only contained `filename_pattern=loan_master_yyyymmdd` and `pattern_kind=template`; `dataset_names`, `connector_name`, and `qualified_names` were missing, and output schema had 8 audit columns. This suggests the config save flow can overwrite hidden binding params that are not part of the visible blueprint `params_schema` or were not present in normalized current params. Local workaround reapplied SOR/connector/dataset binding through `PUT /composition/instances/{id}/params` and patched output schema back to source fields + audit columns. Verified after rebound: `dataset_names=['Loan Master']`, connector `S3-compatible Object Storage Connector`, `lake_format=iceberg_bq_managed`, `columns=29`. Durable fix: preserve hidden binding params during config saves, expose dataset/connector binding as first-class editable/read-only context, and add regression tests that editing filename settings does not drop `dataset_ids` or schema propagation. |
| LCT-030 | 2026-06-17 19:15 ET | FileIngestion has no input port / cannot receive wiring | User tried to port-wire into FileIngestion but there was no orange left-side handle. | Open / temporary local catalog patch applied | Current catalog had `FileIngestion.input_ports=[]` and `output_ports=[raw_output]`; `DagNode` only renders left target handles for `inputPorts.map(...)`, so no left-side orange handle appears. User expectation: FileIngestion needs an input port for this workflow. Local DB hot patch set FileIngestion `input_ports=[{\"name\":\"source_input\",\"description\":\"Optional upstream source/dataset context for file ingestion wiring\"}]`; verification query shows the new input port. Durable fix: update canonical blueprint catalog/spec if FileIngestion is meant to receive upstream source/context wiring, and add UX validation/explanation for source-to-source wiring attempts. |
| LCT-031 | 2026-06-17 19:16 ET | Unwired FileIngestion showed upstream dataset fields | FileIngestion displayed `29 columns` even though there was no wire connecting it to the source/ingest node. | Open / temporary schema reset applied | User provided screenshot `CleanShot 2026-06-17 at 19.15.55.png`; graph showed zero visible edge between `Ingest from MSP` and `File Ingestion`, yet FileIngestion badge showed `+29`. API confirmed `wirings=[]`, FileIngestion had `cols=29`, and hidden `params.dataset_names=['Loan Master']` from earlier workaround. This is misleading: schema badges should reflect visible graph connectivity and/or clearly distinguish hidden dataset binding from upstream wire propagation. Local reset changed FileIngestion `output_schema` back to 8 audit columns until a wire exists; API now shows `wirings=0`, FileIngestion `cols=8`, SnapshotIngestion `cols=8`. Durable fix: recompute/clear schema when wiring state changes, avoid stale/manual schema patches, and show explicit source of schema (`from dataset binding` vs `from upstream wire`) if both are supported. |
| LCT-032 | 2026-06-17 19:22 ET | Graphical pipeline editor feels congested | Drawing a pipeline in the graphical editor feels cramped because the canvas area is small and each node displays too much always-visible state. | Open / UX design needed | User reported the editor feels congested while trying to draw/wire the pipeline on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`. This is distinct from default zoom LCT-012: even at a workable zoom, nodes carry labels, type text, parameter/schema/audit badges, DQ controls, handles, and status pills in a limited workspace next to side panels. Acceptance direction: provide a low-state/compact graph mode where nodes are small and wiring-first, move most state into hover/selection/inspector, allow side panels to collapse, and use zoom-fit/minimap/search/focus affordances so authoring feels canvas-first. |
| LCT-033 | 2026-06-17 19:23 ET | Wiring ingestion nodes drops dataset fields and duplicates audit columns | After wiring `Ingest from MSP` to `FileIngestion`, source dropped to 8 audit columns and FileIngestion showed 16 audit columns instead of the expected Loan Master fields plus audit columns. | Open / temporary local backend hot patch applied | User provided screenshot `CleanShot 2026-06-17 at 19.22.45.png`. API/DB confirmed wiring `snapshot_output -> source_input`; failed state was `SnapshotIngestion.snapshot_output=8`, `FileIngestion.source_input=8`, `FileIngestion.raw_output=16`, all audit-only. Root cause: propagation reads dataset schemas/params through JSONB entity mapping paths that can lose arrays, dataset snapshots use top-level `fields` while op-engine expects `columns`, and `add-audit-columns` appended audit columns even when upstream already contained them. Local hot patch normalized raw `params::text` and `schema_snapshot::text`, accepted `fields` as columns, and made audit addition idempotent by name. After `/schema/recompute`, both nodes report 29 columns with 21 Loan Master fields and exactly 8 audit columns. |
| LCT-034 | 2026-06-17 19:30 ET | Original SOR/source node allows DQ authoring | `Ingest from MSP` is the original file/source node from the SOR but still shows `+ Add DQ`; it should be read-only for DQ authoring. | Open / DQ affordance gate too broad | User provided screenshot `CleanShot 2026-06-17 at 19.30.41.png` showing `+ Add DQ` on `SnapshotIngestion`. Product rule: the original SOR/source-read node is a read-only node of the DAG/source contract and should not allow authoring DQ rules there. Current `dag-view.tsx` allows DQ on every `INGESTION`, `TRANSFORM`, or `MODELING` category, so `SnapshotIngestion` passes the gate. Durable fix: refine the DQ affordance policy by blueprint role/source ownership, not category only. Hide/disable `+ Add DQ` for SOR-original/source-root nodes and expose DQ on materialized pipeline-owned outputs where user-authored checks are valid. |
| LCT-035 | 2026-06-17 19:32 ET | DQ expectation editor lacks column selector and AI rule suggestions | Feature request: the DQ Expectations dialog requires hand-typing column names and has no AI button to suggest/populate rules from the dataset schema. | Open / feature request, not a bug | User provided screenshot `CleanShot 2026-06-17 at 19.31.55.png` on `DQ Expectations — File Ingestion`, then clarified this is technically a feature request, not a bug. Column fields render as plain text inputs with placeholder `column_name`; there is no schema-aware column selector and no `Suggest DQ rules` / AI action. Code confirms `expectation-picker.tsx` defines `col` as `type:"string"` and renders all non-boolean params with `<Input>`. Desired enhancement: use the existing column selector UI component for single/multi-column params, source options from the selected node's schema, add an AI `Suggest DQ rules` button, design/improve the LLM prompt, and auto-populate proposed expectations for review/save. |
| LCT-036 | 2026-06-17 19:36 ET | Saved DQ rules do not reopen and node still says `+ Add DQ` | After saving a DQ rule on FileIngestion, the user cannot pull the saved rule back up, and the node still shows `+ Add DQ` instead of indicating rules already exist. | Open / temporary local frontend hot patch applied | Composition API shows `FileIngestion.dqExpectations` contains one rule, but `CompositionPanel` reopened the dialog from legacy `params.dq_expectations`, which is empty. The DAG node label did not receive a DQ expectation count, so it always rendered `+ Add DQ`. Raw DB also shows the saved rule has empty `kwargs`, and API renders `kwargs` as `{empty:true,traversableAgain:true}`, so the broader JSONB/validation bug remains. Local frontend hot patch now passes canonical `dqExpectations` into `ExpectationPicker` and labels the node `DQ rules (n)` when rules exist. Durable fix: read/write canonical `dqExpectations` everywhere, validate required params before save, repair JSONB expectation payload mapping, and ensure saved rules reopen intact. |
| LCT-037 | 2026-06-17 19:40 ET | FileIngestion has no add/drop column controls | FileIngestion has no visible option to add or drop columns while configuring the pipeline. | Open / column-shaping authoring gap | User reported that FileIngestion offers no way to add/drop columns. Code/spec search shows the platform has `add-column` and `drop-columns` ops, BronzeToSilverCleaning exposes `drop_columns`, and schema propagation has universal `derived_columns` / `dropped_columns` addenda, but FileIngestion's V153 schema behavior is only `read-source -> add-audit-columns -> write-sink(bronze)` and its param surface does not expose column-shaping controls. Durable fix: decide whether FileIngestion itself should expose column-shaping controls for landed data or should provide an explicit "add cleaning/derive/drop-columns step" affordance. In either case, the user should not be stuck with no visible path to add/drop columns after ingestion. |
| LCT-038 | 2026-06-17 19:40 ET | Ports-Schema panel says 29 columns but visibly shows only a few | The right inspector shows 29 input/output columns for Bronze-to-Silver Cleaning, but the visible attribute list only shows about five rows, making it look like most columns are missing. | Fixed locally / verified | User provided screenshots `CleanShot 2026-06-17 at 19.39.42@2x.png` and `CleanShot 2026-06-18 at 01.07.38@2x.png`. API confirms Bronze-to-Silver Cleaning output has all 29 columns, so this is not schema propagation loss. Fix: `ColumnList` in `frontend/src/app/pipelines/[pipelineId]/page.tsx` now uses `max-h-[22.5rem]`, exactly 2.5x the previous `max-h-36` height. Verification: focused frontend lint passed for `src/app/pipelines/[pipelineId]/page.tsx`. Remaining UX polish: add per-section counts/search or a full schema table later if 29+ columns still feel cramped. |
| LCT-039 | 2026-06-17 19:41 ET | Edit Step crashes Bronze-to-Silver Cleaning | Clicking `Edit Step` on Bronze-to-Silver Cleaning crashes with `displayCols.map is not a function` in `MultiColumnPicker`. | Open / temporary local frontend guard applied | User provided screenshot `CleanShot 2026-06-17 at 19.40.43.png`. Stack points to `frontend/src/components/pipeline/column-picker.tsx:278` from `configure-transform-dialog.tsx`. Bronze-to-Silver params_schema uses `ui_construct:"column-picker (multi)"`, so `MultiColumnPicker` receives `upstreamSchema`; at runtime `columns` was not an array and `.map` crashed. Local hot patch normalizes `columns` with `Array.isArray` in `ColumnPicker` and `MultiColumnPicker`; lint passed. Durable fix: ensure `useUpstreamSchema` and upstream-schema API always return `SchemaColumn[]`, add runtime guards for all schema-bound controls, and add a regression that editing BronzeToSilverCleaning with malformed/empty upstream schema does not crash. |
| LCT-040 | 2026-06-17 19:44 ET | Browser annotation tool does not persist submitted annotations | The browser `Annotate` feature lets the user select arbitrary UI elements, including app menu elements, but after typing/submitting an annotation nothing is recorded or actionable. | Open / test-tooling issue, not PULSE product bug | User reported annotation mode can select any element and appears to accept typed annotation text, but the annotation is not recorded. This affects the live QA workflow because browser comments are intended to provide precise UI evidence. Classification: Codex/in-app browser annotation tooling issue, not a PULSE application bug unless reproduced inside PULSE-owned annotation code. Durable fix direction for tooling owner: restrict or clarify selectable targets, confirm submit action persists the annotation, and surface submitted comments reliably in the transcript with target metadata and screenshot. |
| LCT-041 | 2026-06-17 19:46 ET | Edit Step crashes FileIngestion after guard patch | Clicking `Edit Step` on FileIngestion crashes with `(intermediate value)(intermediate value)(intermediate value).map is not a function` in `configure-transform-dialog.tsx` while rendering enum options. | Open / temporary local dialog guards applied | User provided screenshot `CleanShot 2026-06-17 at 19.46.34.png`. Stack points to `frontend/src/components/pipeline/configure-transform-dialog.tsx:1022`, where enum rendering calls `.map` over computed options. This is the same bug class as LCT-039: metadata/schema values arriving at generic config controls are not guaranteed to be arrays at runtime. Local hot patch added `asArray(...)` guards around enum options, blueprint output ports, upstream schema mapping, SQL/filter/chain column inputs, and `useUpstreamSchema`; touched-file lint passed. Browser verification after patch: FileIngestion, SnapshotIngestion, and BronzeToSilverCleaning edit dialogs all render without a Next.js runtime overlay. Durable fix: normalize backend metadata/JSONB DTOs at the API boundary, validate blueprint `params_schema` shape, keep frontend defensive guards, and add an edit-dialog smoke test across all seeded/catalog blueprints. |
| LCT-042 | 2026-06-17 19:55 ET | Bronze cleaning mapper controls are misleading and over-validate | `Trim Columns` needs a convenience action to select all string-like columns. `Fill Null Map` should use a column selector on the left and fill-value input on the right. `Rename Map` showed stale `true` target values and duplicate-target errors before the user typed rename targets. | Open / temporary local UI hot patch applied | User provided screenshots `CleanShot 2026-06-17 at 19.50.27.png`, `CleanShot 2026-06-17 at 19.54.14.png`, and `CleanShot 2026-06-17 at 19.55.34.png`. Root cause: the shared `RenameMapper` component is reused for rename, type-cast, and fill-null maps, but its labels and validation were rename-centric. Duplicate target-name validation is correct for `rename_map`, but wrong for `fill_null_map`, where many columns may share the same fill value such as `true`. Local hot patch added a `Select string columns` bulk action to `MultiColumnPicker`, parameter-specific mapper labels (`Column`, `Fill value`, `Column to rename`, `New column name`), per-param mapper reset keys, and disabled duplicate-target validation for value/fill-null mode. Verification: lint passed for touched files; unit tests passed for column picker and rename mapper, including duplicate fill values. Durable fix: formalize mapper variants in the construct library/spec, ensure each param has semantically correct labels/validation, and add visual/browser smoke coverage for BronzeToSilverCleaning params. |
| LCT-043 | 2026-06-17 20:05 ET | JSONB placeholder keys leak into config controls | Bronze cleaning config showed meaningless rows/values derived from `empty` and `traversableAgain`, for example target/fill values defaulting to `true`. | Open / temporary local frontend quarantine applied | User reported "empty traversable again" values on the screen. This is another manifestation of the JSONB mapping bug seen in LCT-020, LCT-021, LCT-036, and LCT-041: empty JSON collections can arrive at the frontend as `{empty:true,traversableAgain:true}`. Local hot patch added `isJsonCollectionPlaceholder(...)` in `configure-transform-dialog.tsx` so that shape serializes/parses as `{}` or `[]` instead of real param keys. Verification: exact browser check showed `hasTraversableAgain=false`, no raw `empty -> true` row, no duplicate-target error, no rogue mapper inputs, and no runtime overlay. Durable fix: repair JSONB/entity/DTO mapping at backend boundaries and add a regression that empty object/array defaults do not render as editable keys. |
| LCT-044 | 2026-06-17 20:11 ET | Target sink credential dialog crashes and leaks JSONB placeholders | Clicking credentials for target sink `/targets/01KVC126VJRGPDHQQM6DZCMX1Z` crashed with `Rendered more hooks than during the previous render`; after unblocking, the dialog rendered fake `Empty` / `TraversableAgain` credential fields. | Open / temporary local frontend hot patch applied | User provided screenshot `CleanShot 2026-06-17 at 20.11.33.png`. Stack pointed to `frontend/src/components/sor/credential-dialog.tsx:159` inside `CredentialDialog`, rendered from `frontend/src/app/targets/[targetId]/page.tsx`. Local patch removed unnecessary `useTransition` from the dialog fetch path, normalized connector spec `required` and `properties`, guarded enum options, and suppressed `{empty,traversableAgain}` placeholder objects from credential spec properties. Verification: `npm run lint -- src/components/sor/credential-dialog.tsx src/app/targets/[targetId]/page.tsx` passed; browser reload + Dev credential click produced no runtime overlay and no placeholder fields. Follow-on testing exposed that the same target connector was bound to the wrong source connector definition and showed platform/storage-derived copy; tracked separately as LCT-045. Durable fix: repair backend JSONB DTO/entity mapping for connector `connectionSpec`, add runtime schema validation at API boundary, and add target/producers credential-dialog smoke tests. |
| LCT-045 | 2026-06-17 20:20 ET | MongoDB target sink uses wrong connector metadata and credential semantics | A MongoDB data sink asked for lake/S3 output path and showed storage-backend/platform credential copy. The Mongo credential UI also conflated connector metadata with Secret Manager lookup inputs. | Open / temporary local metadata + UI hot patch applied | User provided screenshot `CleanShot 2026-06-17 at 20.13.36.png` and clarified that PULSE should collect Secret Manager lookup inputs for Mongo credentials: Secret Manager URI, optional regional-secret location, and credential secret reference/pointer. DB showed target connector instance `01KVC12RQSWHQNRYYT8KR1BEWK` was attached to source definition `01JCONN0SRC0MONGODB00001` even though destination definition `01JCONN0DST0MONGODB00001` exists. Root causes found: `AddConnectorDialog` hardcoded connector catalog filtering to `SOURCE`, target page did not pass credential strategy to `CredentialDialog`, `AddSinkDialog` mapped unknown destinations to `LakeWriter`, and `ConnectorCatalogController` returned malformed `connectionSpec` unless raw JSONB was normalized. Local fixes: target Add Connector now filters `DESTINATION`; existing local Mongo connector instance was repointed to destination definition; Mongo maps to `DatabaseWriter`; add-sink config shows `Target Collection` instead of `Output Path`; credential dialog shows `Secret Manager URI`, `Location (optional for regional secrets)`, and `Credential Secret Reference`, with no `Secret value`/`Secret reference` mode dropdown. Durable fix: backend should enforce source/target connector-definition compatibility, remove name-heuristic blueprint selection in favor of connector capability metadata, remodel Mongo credential metadata around Secret Manager lookup inputs, and add smoke tests for Mongo target credentials and add-sink flow. |
| LCT-046 | 2026-06-17 20:50 ET | DAG node click reopens right inspector after explicit close | Closing the right step inspector immediately reopens, and a single click on a DAG blueprint node opens the inspector when the user expects single-click selection and double-click open. | Open / temporary local frontend hot patch applied | Repro on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`: open FileIngestion inspector, close it, then click a graph node. Root cause: selected node state was directly coupled to `WorkspaceStepInspector` rendering, and `onCompositionLoaded` auto-selected a node, so any selection/reload could reopen the right region. Local fix separates `selectedWorkspaceInstanceId` from `workspaceInspectorOpen`; outline clicks still open inspector, DAG single-click only selects, and DAG double-click explicitly opens inspector. Verification: lint passed for `dag-view.tsx`, `composition-panel.tsx`, and `page.tsx`; browser check confirmed reload starts with no inspector, single-click does not open it, double-click opens it, close removes it, and another single-click leaves it closed. Durable fix: add component/browser regression coverage for selected-vs-open inspector behavior and decide whether read-only revisions should still allow selection/focus independent of editability. |
| LCT-047 | 2026-06-17 20:52 ET | Add Data Sink credential-status gate disagrees with configured Dev credentials | In Add Data Sink, selecting Target / CCODS shows `Credentials need attention for Dev` for the MongoDB Connector even though Dev credentials were already configured. | Open / logged only; defer fix until after testing | User provided screenshot `CleanShot 2026-06-17 at 20.51.40.png` from `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E` showing the red badge in the Add Data Sink connector-selection step. Expected: if Dev credentials exist and are valid enough for the target connector row, Add Data Sink should not block or warn as if Dev credentials need attention. Likely root-cause areas: credential status derivation is inconsistent between target detail connector rows and Add Sink dialog; Add Sink may be checking a stale connector definition/id, wrong environment key casing, missing credential strategy after the Mongo source-to-destination repair, or credential records tied to the pre-repointed source connector instance. Durable fix: trace the Add Sink credential status query/DTO and compare it with the target detail credential-status source; add a regression where a MongoDB target connector with Dev credential reference appears selectable without a false warning. |
| LCT-048 | 2026-06-17 20:55 ET | MongoDB Add Sink write modes use generic relational labels | Add Data Sink shows `Append`, `Overwrite`, and `Merge` for a MongoDB sink; user questioned whether those options and labels are valid, and noted SQL Server-style `overwrite` should be described as `truncate and reload`. | Open / researched and logged only | Local code hardcodes `["append","overwrite","merge"]` for every sink in `frontend/src/components/pipeline/add-sink-dialog.tsx`. MongoDB Spark Connector docs say batch save modes are `append` and `overwrite`; overwrite drops/recreates the collection by default, while `truncateMode=TRUNCATE` deletes entries and preserves indexes/options/sharding. MongoDB write operations are document-oriented (`insert`, `replace`, `update`) and can upsert using id fields. Expected: connector-specific write modes and labels. For MongoDB, use document/collection semantics such as `insert documents`, `replace/upsert by key`, `update by key`, and explicit `drop and reload` or `truncate and reload` if destructive reload is allowed; do not show generic `merge` unless it maps to a clear Mongo update/upsert mode with key fields. For SQL/JDBC, rename user-facing `overwrite` to `truncate and reload` where that is the actual behavior. |
| LCT-049 | 2026-06-17 20:55 ET | New DAG nodes are placed on a second row, creating avoidable drag-to-connect work | Creating a new node places it on a second row, so the user has to drag it back up to the first row before connecting it to the existing pipeline. | Open / logged only; defer fix until after testing | Repro on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`: add a new pipeline step/node from the graphical editor. Observed: new node appears in a lower row instead of near the selected/source context or the expected continuation point. Expected: adding a step should place it near the selected node, at the next logical position in the flow, or auto-layout it into the same row with enough spacing to wire immediately. Durable fix: inspect `CompositionPanel` add-step flow and `DagView` layout algorithm; make new-node placement context-aware, support `insert after selected`, and/or run an auto-layout pass that reduces manual repositioning. |
| LCT-050 | 2026-06-17 20:58 ET | DAG single-click selection is not visually obvious enough to support `Edit Step` | After changing DAG nodes so double-click opens the side panel, a single-click did not clearly show which node was selected, so it was not obvious that `Edit Step` would edit that node. | Open / temporary local frontend hot patch applied | Repro on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`: close/right-panel workflow from LCT-046, then single-click a DAG node and look for selected-state feedback before clicking `Edit Step`. Local fix: `DagNode` now uses React Flow's `selected` prop to render a primary ring and `Selected` badge; workspace toolbar button label changed from `Edit Step` to `Edit Selected Step` with a title naming the selected step. Verification: `npm run lint -- src/components/pipeline/dag-node.tsx src/components/pipeline/composition-panel.tsx` passed; browser check confirmed a single-click leaves the inspector closed, keeps `Edit Selected Step` visible, and visibly marks the selected DAG node. Durable fix: add regression/UI test for single-click selection affordance and consider an even clearer selected-node summary in the toolbar. |
| LCT-051 | 2026-06-17 20:59 ET | DAG wiring has no discoverable delete affordance | User tried to delete the wire between `Bronze-to-Silver Cleaning` and `Write to MongoDB Connector`, but there is no visible option/control to remove the wiring. | Open / logged only | Repro on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`: inspect the wire between Bronze-to-Silver Cleaning and the MongoDB sink. Code evidence: backend/API delete path exists via `DELETE /api/v1/versions/{versionId}/composition/wirings/{wiringId}` and `CompositionPanel.handleUnwire`; `DagView` only exposes it through ReactFlow `onEdgesDelete`, which depends on implicit edge selection + keyboard deletion. Expected: a visible, discoverable delete action for a selected wire, such as an edge toolbar/trash button, context menu, details panel action, or keyboard-hint plus confirmation. Durable fix: add explicit edge selection state and a visible `Delete wire` affordance that calls existing `handleUnwire`, then verify schema recomputation after removal. |
| LCT-052 | 2026-06-17 21:00 ET | PII Masking required dropdowns are empty and save validation is missing | In `PII Masking & Tokenization`, the masking strategy/hash algorithm dropdown is not populated, yet the screen allowed saving even though required fields were missing. | Open / logged only; defer fix until after testing | Repro on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`: add/open PIIMasking config and inspect masking strategy/hash algorithm fields. User expectation: hash-related options should be populated; required fields should block save until completed. Code/catalog evidence: `V153__builder_op_lists_and_param_tiering.sql` defines `PIIMasking.masking_strategy` as required enum with options `["hash","redact","tokenize","encrypt"]` and `hash_algorithm` options `["sha256","sha512","md5"]`; `V155` only adds a column-picker hint for `columns_to_mask`. Likely root cause: live blueprint metadata is being normalized/clobbered before rendering, or generic enum rendering/required validation in `configure-transform-dialog.tsx` is not enforcing required values. Durable fix: verify live DB `params_schema` for PIIMasking, restore enum options if missing, make required enum/string/string[] params block save, and add a regression for PIIMasking config with empty required fields. |
| LCT-059 | 2026-06-18 00:39 ET | Deleting a DAG wire leaves stale downstream schemas/column counts | After deleting the wire into `FileIngestion`, the wire disappeared but the node still showed `37 cols` and `audit: 16`. | Fixed locally / verified | Repro on `/pipelines/01KVCFX9KHVBBEMSTCKRHGDX3G`: delete the wire into `FileIngestion`. DB confirmed `port_wirings` had zero rows, but stale `instance_port_schemas` still had `FileIngestion.source_input=29` and `raw_output=37`; `sub_pipeline_instances.output_schema` still had 37 columns while `schema_status=conflict`. Root causes: `AddAuditColumnsOp` was non-idempotent, `FileIngestion` used source-root audit semantics even when consuming an already-audited upstream schema, and `SchemaPropagationService.processInstance` emitted `MISSING_UPSTREAM` without clearing stale input/output port rows or mirrored `output_schema`. Fix: `AddAuditColumnsOp` now skips audit names already present; upstream-wired `FileIngestion` filters `add-audit-columns` out of the op-list before engine execution; missing-upstream propagation clears stale input/output port schemas and mirrors empty `output_schema`. Verification: focused Gradle tests passed (`SchemaOpEngineTest`, `SchemaPropagationServiceTest`); backend restarted healthy; `POST /api/v1/versions/01KVCFX9KKHDW5WN4PJ1WZ8T2K/schema/recompute` returned `processed=2, conflicts=1`; composition API now reports `SnapshotIngestion outputCols=29 auditCols=8`, `FileIngestion schemaStatus=conflict outputCols=0 auditCols=0`, with no stale FileIngestion port schema rows. |
| LCT-060 | 2026-06-18 01:01 ET | Bronze-to-Silver Cleaning still exposed Drop Null Columns after audit | `Drop Null Columns` was still visible in the Bronze-to-Silver Cleaning edit panel even though the audit/fix pass had claimed it was removed. | Fixed locally / verified | User provided screenshot `CleanShot 2026-06-18 at 01.01.10.png` on `/pipelines/01KVCHEX2CR0CZZWCRTD1CQ9YT`. Root cause: the earlier local frontend patch only hid the field; the durable catalog still had `drop_null_columns` in `BronzeToSilverCleaning.params_schema`, and its `schema_behavior.ops` still had a `filter-rows` op configured from `drop_null_columns`. Fix: added Flyway V162 to remove `drop_null_columns` from active Bronze-to-Silver blueprint metadata, remove the matching `filter-rows` op from schema behavior, and strip stale `drop_null_columns` params from existing Bronze-to-Silver instances. Verification: focused Gradle tests passed (`SchemaOpEngineTest`, `SchemaPropagationServiceTest`); backend `/actuator/health` is `UP`; Flyway history shows V162 success; DB and blueprint API now expose only `drop_columns` and `derived_columns` for shaping params, with zero drop-null ops and zero existing Bronze-to-Silver instances carrying `drop_null_columns`. Audit miss note: prior audit verified the UI symptom, not the canonical catalog source, so this class needs DB/API-level assertions for catalog-driven controls. |
| LCT-061 | 2026-06-18 01:04 ET | Bronze-to-Silver Cleaning derived lake_format still defaults to Delta | Saving/configuring Bronze-to-Silver Cleaning showed `Illegal lake_format=delta for backend=GCP, layer=silver` even after the prior lake-format audit claimed Delta was removed. | Fixed locally / verified | User provided screenshot `CleanShot 2026-06-18 at 01.04.30.png` on `/pipelines/01KVCHEX2CR0CZZWCRTD1CQ9YT`. Root cause: V160 used `status = 'ACTIVE'`, but live active blueprint rows use lowercase `active`, so active catalog rows still had `lake_format.default=delta` and options including `delta`. The config dialog also received only `params`, not canonical instance storage columns, so it could not auto-correct derived storage fields from the actual `storageBackend/lakeLayer/lakeFormat`. Fix: added Flyway V163 to repair all active blueprint `lake_format` params case-safely, remove `delta` from options, replace Delta defaults with `iceberg_bq_managed`, repair existing canonical instance columns carrying Delta, and strip stale mirrored `params.lake_format=delta`; updated the config dialog to receive canonical storage fields and auto-correct invalid `lake_format` values using the same DPC/GCP matrix as the backend. Verification: frontend lint passed for `configure-transform-dialog.tsx` and `composition-panel.tsx`; focused Gradle tests passed; backend restarted healthy with Flyway V163 applied; DB reports zero active `lake_format` params with Delta default/options; Bronze-to-Silver API now returns default `iceberg_bq_managed` and no Delta option; save-path probe for instance `01KVCHJWBTPSV9GSYEWX520HJK` persisted `GCP/silver/iceberg_bq_managed` with no validation error. Audit miss note: prior audit looked at validator/system-prompt changes and missed live catalog row casing plus frontend canonical-field propagation. |
| LCT-062 | 2026-06-18 01:15 ET | DAG needs an explicit auto-layout control | User requested an auto-layout button on the DAG screen that uses dagre to optimally lay out the graph once the user is done building the pipeline. | Fixed locally / verified | Implemented a visible `Auto layout` button beside the existing `Compact` control in `DagView`. It uses `@dagrejs/dagre` left-to-right layout over current React Flow nodes/wires, updates node positions, and refits the viewport. During implementation, a ReferenceError occurred because `fitViewOptions` was referenced before initialization; fixed by moving `fitViewOptions` above the callback. Verification: `npm run lint -- src/components/pipeline/dag-view.tsx` passed; Playwright smoke with a dev token reached `/pipelines/01KVCHEX2CR0CZZWCRTD1CQ9YT`, confirmed `Auto layout` is visible, found 5 DAG nodes, clicked the button, and saw no ReferenceError/page crash. |
| LCT-063 | 2026-06-18 01:25 ET | Pipeline workspace middle section scrollbar should always be visible | User requested the vertical scrollbar on the pipeline page/workspace middle section always be visible. | Fixed locally / verified | Updated the pipeline workspace grid in `frontend/src/app/pipelines/[pipelineId]/page.tsx` to use `overflow-y-scroll`, `scrollbarGutter: stable`, and a bounded `max-h-[72vh]` while preserving the existing `min-h-[640px]`. Verification: `npm run lint -- 'src/app/pipelines/[pipelineId]/page.tsx'` passed. |

## Root-Cause Research Queue

These are the current high-signal issues from the fixed-build test pass. Each entry is written so a follow-on agent can start from concrete repro + code pointers instead of re-discovering the bug.

### LCT-012 — Pipeline DAG Opens Too Zoomed In

- Repro: open `/pipelines/01JDEMO0UL0CARDTXN000001` on build `45a7359`; inspect the graphical DAG in the workspace/canvas region.
- Observed: initial canvas zoom is too large for the seeded Card Transaction Curation DAG.
- Expected: the 5-step seeded DAG should fit comfortably by default, with labels/nodes readable and no immediate need to zoom out.
- Primary file: `frontend/src/components/pipeline/dag-view.tsx`.
- Evidence: `DagView` full-screen/fill-parent mode forcibly calls `instance.setViewport({ x: 80, y: 100, zoom: 2.0 })` in `handleInit` (`dag-view.tsx:329-340`).
- Root-cause hypothesis: the remediation intentionally overrode ReactFlow `fitView` to force larger nodes for one-row DAGs, but the fixed `2.0` zoom is not responsive to graph size or viewport size.
- Investigation path: compare embedded vs workspace `fillParent` usage from `CompositionPanel`; test `fitView` with padding/minZoom, or compute initial bounds/zoom from node count and viewport dimensions. Add a regression around Card Transaction Curation or mock a 5-node horizontal DAG.

### LCT-013 — Outline Selection Does Not Navigate Graph View

- Repro: open `/pipelines/01JDEMO0UL0CARDTXN000001`; click a step in the left Outline.
- Observed: selection/inspector behavior exists, but the graphical DAG does not pan/center/zoom to the selected node.
- Expected: outline click should select the corresponding graph node, center/pan it into view at a comfortable zoom, and populate the right config panel.
- Primary files: `frontend/src/app/pipelines/[pipelineId]/page.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-view.tsx`.
- Evidence: `PipelineOutline` calls `onSelect(instance.id)` (`page.tsx:1001-1005`), page passes this through as `selectedInstanceId` (`page.tsx:734-748`), and `DagView` only marks selected nodes (`dag-view.tsx:232-240`). There is no `ReactFlowInstance.setCenter`, `fitView({ nodes })`, or viewport focus effect keyed by `selectedInstanceId`.
- Related risk: `CompositionPanel.handleInstanceClick` returns early when `!editable` (`composition-panel.tsx:209-216`), so canvas node selection may be blocked on non-ENGINEERING revisions even when inspector selection should be read-only.
- Root-cause hypothesis: state plumbing was added for selected-instance inspector, but viewport control was not added to `DagView`; node click editability and selection concerns are conflated.
- Investigation path: add a `useEffect` in `DagView` keyed by `selectedInstanceId` plus `nodes` and call ReactFlow viewport API to center the selected node. Separate read-only selection from edit-only configuration mutations.

### LCT-014 — Right Config Panel Is Read-Only

- Repro: open `/pipelines/01JDEMO0UL0CARDTXN000001`; select `Publish Spend Fact to Lake`; right panel opens on Config tab.
- Observed: fields render as static rows/pre blocks; user cannot edit values.
- Expected: the right panel should be the editable configuration surface, or at minimum expose a clear edit action that opens the editable form for the selected node.
- Screenshot: `CleanShot 2026-06-17 at 18.27.36@2x.png`.
- Primary files: `frontend/src/app/pipelines/[pipelineId]/page.tsx`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`.
- Evidence: `WorkspaceStepInspector` renders config with `InspectorRow` and `<pre>` for params (`page.tsx:1088-1105`) and has no save path. The editable metadata-driven form/save path exists in `ConfigureTransformDialog`, which calls `PUT /api/v1/versions/{versionId}/composition/instances/{instanceId}/params` (`configure-transform-dialog.tsx:519-533`).
- Root-cause hypothesis: remediation implemented a right-side inspector shell, but did not port/reuse the existing editable config form into the panel.
- Investigation path: extract the metadata-driven editor from `ConfigureTransformDialog` into a shared component usable in the right panel, or embed the dialog form body in `WorkspaceStepInspector`. Preserve selected node context and refresh composition after save.

### LCT-015 — Create Pipeline Missing Derived Storage Backend

- Repro: open `/pipelines`, click New Pipeline, create `Pipeline 1` under Unsecured Lending / Domain 1.
- Observed: UI shows `defaultStorageBackend is required when creating a pipeline; must be 'DPC' or 'GCP'`.
- Expected: no storage-backend error. PULSE already runs globally as either DPC or GCP; pipeline creation should derive the default storage backend from that global runtime/environment authority.
- Screenshot: `CleanShot 2026-06-17 at 18.29.05.png`.
- Primary files: `frontend/src/components/pipeline/create-pipeline-dialog.tsx`, `backend/src/main/java/com/pulse/pipeline/service/PipelineService.java`, runtime authority files under `backend/src/main/java/com/pulse/runtime/`.
- Evidence: frontend POST body includes only `name`, `description`, `domainName`, `domainId` (`create-pipeline-dialog.tsx:57-66`). Backend requires `request.defaultStorageBackend()` and throws on null/blank (`PipelineService.java:96-107`). Backend boot logged runtime authority as `persona=GCP_PULSE` and `storageBackends=[GCP]`, so the value is knowable server-side in this run.
- Workaround executed: direct POST with `defaultStorageBackend: "GCP"` created pipeline `01KVBVCFXGPB1T5TQGWTDR826E` and active version `01KVBVCFXH8E3GBAWN6T1YJ8BX`.
- Root-cause hypothesis: ARCH-010 made `defaultStorageBackend` mandatory at API boundary, but the UI create flow and/or backend create service was not updated to derive it from the global runtime persona.
- Investigation path: decide source of truth. Prefer backend derivation from `RuntimeAuthorityService` to avoid every client duplicating mode logic; optionally include derived value in frontend payload for display/audit. Add create-pipeline regression for GCP_PULSE and DPC_PULSE modes.

### LCT-016 — Producer Credential Dialog Crashes On `required`

- Repro: open `/producers/01KVBVEAWY355TZEY19PN6V1HV`; open the credential dialog for the connector shown by the user.
- Observed: Next.js runtime overlay: `TypeError: spec?.required?.includes is not a function`.
- Expected: dialog renders credential fields or a helpful malformed-spec/platform-resolved message; no runtime crash.
- Screenshot: `CleanShot 2026-06-17 at 18.35.00.png`.
- Primary files: `frontend/src/components/sor/credential-dialog.tsx`, `frontend/src/app/producers/[sorId]/page.tsx`, connector definition/spec seed or API serving credential specs.
- Evidence: crash points at `const isRequired = (key: string) => spec?.required?.includes(key) ?? false;` (`credential-dialog.tsx:295`). Dialog maps `spec.properties` at `credential-dialog.tsx:538-545`. Parent renders dialog from producer page at `page.tsx:827-834`.
- Root-cause hypothesis: credential spec `required` is not normalized to a JSON-Schema-style `string[]`; it may be an object, boolean, string, or another schema variant. The frontend assumes array shape and crashes.
- Investigation path: inspect connector definition returned for producer `01KVBVEAWY355TZEY19PN6V1HV`, especially `connection_specs.required`. Add frontend guard `Array.isArray(spec?.required) ? spec.required.includes(key) : false`, then decide whether backend should normalize seeded connector specs. Add a unit test in/near `credential-dialog` for non-array `required`.

### LCT-017 — Define Dataset Missing First-Class Sample Upload Option

- Repro: open `/producers/01KVBVEAWY355TZEY19PN6V1HV`; click Define Dataset; observe the first method-selection screen.
- Observed: choices are `Infer from File Path`, `Upload Schema`, and `Manual Field Definition`. There is no obvious `Upload Sample File` option.
- Expected: user should be able to choose local sample upload directly from the first method screen, especially for file-backed datasets where sample-driven schema inference is a natural path.
- Screenshot: `CleanShot 2026-06-17 at 18.36.21.png`.
- Primary files: `frontend/src/components/sor/define-dataset-dialog.tsx`, `backend/src/main/java/com/pulse/sor/controller/DatasetDefinitionMethods.java`, `backend/src/main/java/com/pulse/sor/controller/SchemaDiscoveryController.java`, `backend/src/main/java/com/pulse/sor/service/SchemaDiscoveryService.java`.
- Evidence: backend file methods only expose `FILE_INFERENCE`, `SCHEMA_UPLOAD`, and `MANUAL_DEFINITION` (`DatasetDefinitionMethods.java:52-57`). Frontend has an upload button under the `FILE_INFERENCE` configuration step (`define-dataset-dialog.tsx:411-441`) and another upload route under `SCHEMA_UPLOAD` after selecting `CSV_SAMPLE` (`define-dataset-dialog.tsx:334-389`), but neither is visible as a first-screen option. Backend sample discovery exists (`SchemaDiscoveryController.java:86-94`) and service supports deterministic CSV/JSON sample inference (`SchemaDiscoveryService.java:138-166`).
- Root-cause hypothesis: capability exists partly in later steps/API, but method taxonomy and first-screen labels hide it. Users expect sample upload as its own schema-definition method, not as a secondary action buried under file path or schema upload.
- Investigation path: add a `SAMPLE_UPLOAD` / `SAMPLE_INFERENCE` method for file connectors, or rename `FILE_INFERENCE` to include local sample upload and show both path and upload affordances on the method card. Wire first-screen selection to the existing sample discovery endpoint rather than only parsing headers client-side.

### LCT-018 — Uploaded Sample File Is Saved As Draft With No Schema

- Repro: open `/producers/01KVBVEAWY355TZEY19PN6V1HV`; Define Dataset; choose `Infer from File Path`; upload `loan_master.csv`; complete dataset creation.
- Observed: no file preview is shown, no apparent LLM/PII classification call happens, and the created dataset card says `No schema defined`.
- Expected: after sample upload, user should see a preview of parsed rows/headers, schema inference should run, PII/classification should be evaluated, and the resulting dataset should persist a schema snapshot instead of becoming a draft with no schema.
- Screenshot: `CleanShot 2026-06-17 at 18.40.43.png`.
- Primary files: `frontend/src/components/sor/define-dataset-dialog.tsx`, `backend/src/main/java/com/pulse/sor/controller/DatasetController.java`, `backend/src/main/java/com/pulse/sor/controller/SchemaDiscoveryController.java`, `backend/src/main/java/com/pulse/sor/service/SchemaDiscoveryService.java`, any PII/classification service wired to dataset/schema discovery.
- Evidence: database row for dataset `01KVBVTNG3QZNGX0F5Z881VVTM` (`Loan Master`) has `definition_type=FILE_INFERENCE`, `definition_config={"glob":"*","path":"loan_master.csv","format":"csv"}`, `schema_snapshot=NULL`, `status=DRAFT`, `classification=INTERNAL`, `discovery_method=NULL`, `discovery_proof=NULL`. Frontend upload handler for `FILE_INFERENCE` only sets `filePath` to the local filename and derives `fileFormat` (`define-dataset-dialog.tsx:422-436`). Submit path sends only `definitionConfig` for `FILE_INFERENCE` (`define-dataset-dialog.tsx:159-161`). Backend marks datasets `DRAFT` when `schemaSnapshot` is missing (`DatasetController.java:146-150`). Backend sample discovery endpoint exists (`SchemaDiscoveryController.java:86-94`) and service can infer CSV/JSON sample schema (`SchemaDiscoveryService.java:138-166`), but this flow does not call it.
- Root-cause hypothesis: “Upload Sample File” in `FILE_INFERENCE` is currently a filename picker, not a sample-ingestion workflow. It discards file content and treats the filename as a remote path, so there is no preview, no schema snapshot, no PII evaluation, and no discovery proof.
- Investigation path: change `FILE_INFERENCE` upload to read file text for supported local sample formats, render a preview table, POST sample content to `/api/v1/connector-instances/{ciId}/schema-discovery/sample`, persist returned schema/classification/provenance into dataset creation, and disable final save until inference succeeds or user explicitly saves as draft. If PII requires LLM, identify the existing PII/classification service and call it after deterministic schema inference with clear loading/error states.

### LCT-019 — Manual Dataset Create Crashes On Duplicate Qualified Name

- Repro: after LCT-018 creates draft dataset `Loan Master`, open Define Dataset again, choose manual definition, use the same dataset name `Loan Master`, and click Save.
- Observed: Next.js runtime overlay shows generic `An unexpected error occurred`; stack points to `src/lib/api.ts:160` and `define-dataset-dialog.tsx:206`.
- Expected: user should get an inline duplicate-name validation message and a path to edit the existing dataset or pick another name.
- Screenshot: `CleanShot 2026-06-17 at 18.45.17.png`.
- Primary files: `frontend/src/components/sor/define-dataset-dialog.tsx`, `frontend/src/lib/api.ts`, `backend/src/main/java/com/pulse/sor/controller/DatasetController.java`, `backend/src/main/java/com/pulse/common/exception/GlobalExceptionHandler.java`, dataset repository/model.
- Evidence: backend log at `2026-06-17T18:45:09` shows SQLState `23505`, constraint `datasets_qualified_name_key`, duplicate key `tenant-unsecured-lending.domain-1.raw.Loan Master`, thrown from `DatasetController.createDataset(DatasetController.java:91)`. Existing DB row is `01KVBVTNG3QZNGX0F5Z881VVTM`.
- Root-cause hypothesis: create flow relies on DB unique constraint rather than service-level validation; global exception handler maps the data-integrity violation to an opaque 500; frontend does not catch/display API errors inside the dialog.
- Investigation path: add backend duplicate check by `qualifiedName` before save and return 409/ProblemDetail with a clear message. Add frontend `try/catch` around `handleSave` in `DefineDatasetDialog` (currently only `finally`) and render error inline. Consider offering "Edit existing dataset" when the duplicate is in the same connector/SOR.
- Test recovery applied: updated existing dataset `01KVBVTNG3QZNGX0F5Z881VVTM` to `SCHEMA_DEFINED`, `CONFIDENTIAL`, and a 21-field loan-master schema from `SchemaDiscoveryService.loanMasterSchema`.
- Additional research note: direct DB JSONB now has a proper `fields` array, but API response serializes it as `{"fields":{"empty":false,"traversableAgain":true}}`. Investigate `Dataset.schemaSnapshot` mapping (`@JdbcTypeCode(SqlTypes.JSON) Map<String,Object>`) and Jackson/Hibernate JSON collection serialization.

### LCT-020 — API Corrupts Schema Field Array On Read

- Repro: query dataset `01KVBVTNG3QZNGX0F5Z881VVTM` via `GET /api/v1/datasets/01KVBVTNG3QZNGX0F5Z881VVTM` or load `/producers/01KVBVEAWY355TZEY19PN6V1HV` and expand `Loan Master`.
- Observed: UI shows `SCHEMA_DEFINED` but no fields; API returns `"schemaSnapshot":{"fields":{"empty":false,"traversableAgain":true}}`.
- Expected: API returns `"schemaSnapshot":{"fields":[{"name":"loan_id",...}, ...]}` and UI renders the field table.
- Primary files: `backend/src/main/java/com/pulse/sor/model/Dataset.java`, `backend/src/main/java/com/pulse/sor/controller/DatasetController.java`, `frontend/src/app/producers/[sorId]/page.tsx`.
- Evidence: DB `jsonb_typeof(schema_snapshot->'fields')` is `array`, `jsonb_array_length(...)` is `21`, and first field is valid JSON. Producer page reads `const schemaFields = (ds.schemaSnapshot as { fields?: Array<...> })?.fields` and only renders rows when `schemaFields.length > 0`.
- Root-cause hypothesis: Hibernate JSON mapping into `Map<String,Object>` is materializing nested JSON arrays as an internal collection/proxy that Jackson serializes as bean properties (`empty`, `traversableAgain`) rather than a JSON array.
- Investigation path: replace entity JSON mapping with a Jackson `JsonNode`/DTO-safe representation, or normalize `Dataset` responses in `DatasetController` with an explicit DTO produced from raw JSONB/Jackson conversion. Add regression asserting GET dataset returns `schemaSnapshot.fields` as an array after create/update/read.
- Temporary local workaround applied: `backend/src/main/java/com/pulse/sor/controller/DatasetController.java` now injects `JdbcTemplate` and `ObjectMapper`, re-reads `schema_snapshot::text`, parses it into a plain Jackson `Map<String,Object>`, and returns the normalized entity. This is intentionally a test unblock, not the permanent fix, because it mutates a managed entity during read and bypasses the real JSON mapping problem.
- Verification after workaround: `./gradlew compileJava` passed; restarted backend with `--pulse.chat.orchestration=graph`; `/actuator/health` returned `UP`; `GET /api/v1/datasets/01KVBVTNG3QZNGX0F5Z881VVTM` returned `fields_type=list`, `fields_count=21`, first field `{'pii': False, 'name': 'loan_id', 'type': 'VARCHAR', 'nullable': False}`.

### LCT-021 — Pipeline Composition Output Schema Serializes As Non-Array Shape

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, add a `FileIngestion` step to the canvas, then reload or let the page refresh composition.
- Observed: the pipeline page crashes and will not pull up. Next.js overlay shows `TypeError: output.map is not a function` in `computeSchemaDiff` (`frontend/src/lib/schema-diff.ts:23`), called from `DagView.useMemo` (`frontend/src/components/pipeline/dag-view.tsx:186`).
- Expected: adding a step should update the DAG and render the node. If schema metadata is missing or malformed, the DAG should render with no schema diff rather than crash.
- Screenshot: `CleanShot 2026-06-17 at 18.54.28.png`.
- Primary files: `backend/src/main/java/com/pulse/pipeline/model/SubPipelineInstance.java`, `backend/src/main/java/com/pulse/pipeline/controller/CompositionController.java`, `backend/src/main/java/com/pulse/pipeline/service/CompositionService.java`, `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/lib/schema-diff.ts`.
- Evidence: created instance `01KVBWNWKKMPX2QA08R359DY4P` is persisted on version `01KVBVCFXH8E3GBAWN6T1YJ8BX`. DB query shows `sub_pipeline_instances.output_schema` is a valid JSON object with a `columns` array of 8 audit fields. Before workaround, composition API returned `outputSchema` as `{"empty":false,"traversableAgain":true}` for that instance. `SubPipelineInstance.outputSchema` is mapped as `@JdbcTypeCode(SqlTypes.JSON) Map<String,Object>` (`SubPipelineInstance.java:50-52`), matching the same serialization failure pattern as LCT-020.
- Root-cause hypothesis: Hibernate JSON mapping of JSONB into `Map<String,Object>` materializes nested arrays/objects as a type Jackson serializes as bean properties, so `output_schema.columns` is lost in API responses. The frontend schema-diff code also lacks an `Array.isArray` guard and assumes callers always pass `SchemaColumn[]`.
- Temporary local workaround applied: `CompositionController` injects `JdbcTemplate`/`ObjectMapper`, re-reads `output_schema::text` for instances, parses raw JSONB into a plain Jackson map, and returns normalized composition responses.
- Verification after workaround: `./gradlew compileJava` passed; Spring DevTools restarted backend; `/actuator/health` returned `UP`; `GET /api/v1/versions/01KVBVCFXH8E3GBAWN6T1YJ8BX/composition` returned one instance with `outputSchema_type=dict`, `columns_type=list`, `columns_count=8`; `HEAD /pipelines/01KVBVCFXGPB1T5TQGWTDR826E` returned `HTTP/1.1 200 OK`.
- Durable fix direction: replace controller hot patches with DTO-safe JSON handling for every JSONB field exposed through APIs, especially `Dataset.schemaSnapshot` and `SubPipelineInstance.outputSchema`. Add frontend defensive guards in `computeSchemaDiff` or `DagView` so malformed server data is surfaced as a recoverable warning instead of a full-page runtime overlay.

### LCT-022 — FileIngestion Binding Is Not Discoverable And Does Not Propagate Source Fields

- Repro: manually add a `FileIngestion` step to `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, select it in the right-side inspector, and try to link it to the existing `Loan Master` dataset.
- Observed: the existing step has no obvious dataset-binding control in the right inspector. Instance started with `params={}`, `input_datasets=[]`, `output_datasets=[]`. After setting binding params through the API, the composition response serialized `params.dataset_names` as `{"empty":false,"traversableAgain":true}` before the local hot patch, and the output schema stayed at 8 audit columns instead of 21 source fields + audit columns.
- Expected: users should be able to bind a FileIngestion step to a defined dataset from the UI, and the node should show the linked dataset name plus the derived output schema.
- Primary files: `frontend/src/components/pipeline/add-source-dialog.tsx`, `frontend/src/app/pipelines/[pipelineId]/page.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `backend/src/main/java/com/pulse/pipeline/service/CompositionService.java`, `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, `backend/src/main/java/com/pulse/pipeline/controller/CompositionController.java`.
- Evidence: `AddSourceDialog.handleAdd` writes `params.dataset_ids`, `dataset_names`, and `qualified_names` (`add-source-dialog.tsx:150-167`). `CompositionService.tryResolveSchemaFromDatasets` expects `params.dataset_ids` to be a `List` (`CompositionService.java:346-355`). The JSONB mapping bug can prevent arrays from being usable/serializable consistently. Direct DB patch showed desired output shape is possible: source dataset 21 fields plus 8 audit columns, total 29.
- Temporary local workaround applied: updated instance params via API to bind SOR `MSP`, connector `S3-compatible Object Storage Connector`, and dataset `Loan Master`; extended `CompositionController` response normalization to parse `params::text`; patched `sub_pipeline_instances.output_schema` to `dataset.schema_snapshot.fields || audit_columns`.
- Verification after workaround: backend health `UP`; composition API returns `params.dataset_names` as `['Loan Master']`, `params.dataset_ids` as `['01KVBVTNG3QZNGX0F5Z881VVTM']`, and `outputSchema.columns` as a 29-item list starting with `loan_id`, `borrower_name`, `ssn`, `email`, `phone` and ending with PULSE audit columns.
- Durable fix direction: make dataset binding first-class in the right config panel or prevent generic add-step for ingestion and route through Add Data Source. Fix JSONB DTO/entity mapping so `params.dataset_ids` remains a real list server-side and client-side. Add propagation regression that a FileIngestion node bound to a schema-defined dataset emits source fields plus canonical audit columns.

### LCT-023 — File Pattern Kind Dropdown Is Empty And Filename Mnemonic Assist Is Missing

- Repro: open pipeline `01KVBVCFXGPB1T5TQGWTDR826E`, select/configure the `FileIngestion` step, inspect `pattern_kind` and `filename_pattern`.
- Observed: `pattern_kind` dropdown has zero values. `filename_pattern` is a plain text field even though file patterns commonly need `{date}` and a mnemonic/date-format assist.
- Expected: `pattern_kind` should offer valid choices such as `template`, `literal`/`exact`, `glob`, and `regex`; filename pattern should provide an assist to insert `{date}`, choose a date mnemonic (`RUN_DATE`, `PBD`, etc.), choose/override date format, and preview resulting examples.
- Primary files: `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `backend/src/main/resources/db/migration/V155__blueprint_param_ui_construct_hints.sql`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `frontend/src/components/pipeline/mnemonic-date-input.tsx`.
- Evidence: current FileIngestion blueprint row had `{"name":"pattern_kind","type":"enum","default":"template",...}` with no `options`; metadata renderer uses `(definition.options ?? []).map(...)`, so no select items render (`configure-transform-dialog.tsx:1009-1027`). Docs/specs and older migrations describe template/glob/regex options (`V92`, `V93`, `SPEC-blueprint-catalog.md`). `date_value` has `date-mnemonic-picker`, but `filename_pattern` does not, so users have no inline way to compose `loan_master_{date}.csv` with mnemonic/date-format guidance.
- Temporary local workaround applied: updated FileIngestion and FileArrivalSensor blueprint `params_schema` in the local DB to set `pattern_kind.options=["template","literal","glob","regex"]`.
- Durable fix direction: update canonical migration/catalog data to preserve enum options and add a dedicated `filename-pattern-builder`/assist construct. That construct should coordinate `filename_pattern`, `pattern_kind`, `date_value`, and `date_format`, not leave them as unrelated controls.

### LCT-024 — File Date Format Must Be User-Overridable

- Repro: open the FileIngestion config UI and inspect `Date Format`.
- Observed: `Date Format` renders with `string` + `Derived` badges, value `yyyyMMdd`, and `Read-only / Derived from Platform default`.
- Expected: `yyyyMMdd` can be the default suggestion, but users must be able to override it because source files arrive with many filename date formats.
- Screenshot: `CleanShot 2026-06-17 at 19.05.12.png`.
- Primary files: `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`.
- Evidence: FileIngestion `date_format` was seeded as `tier:"derived"` and `derivedFrom:"platform_default"`. Frontend `isDerivedParam` returns true for `tier === "derived" || tier === "system-derived"` (`configure-transform-dialog.tsx:146-148`), so the metadata renderer routes it to `DerivedParamInspector` instead of an editable input.
- Temporary local workaround applied: updated FileIngestion and FileArrivalSensor blueprint `params_schema` in local DB so `date_format` is `tier:"user"`, has no `derivedFrom`, preserves default `yyyyMMdd`, and describes common overrides like `yyyy-MM-dd` and `MMddyyyy`.
- Durable fix direction: classify filename date format as a user parameter in canonical catalog/migrations. Keep platform defaults as initial values/help text, not read-only authority. Include this in the filename-pattern assist from LCT-023.

### LCT-025 — Date Value / Mnemonic Control Needs Product-Language Assist

- Repro: open the FileIngestion config UI and inspect `Date Value`.
- Observed: user does not understand what `Mnemonic`, `ISO date`, and `Free-text` are trying to achieve.
- Expected: the control should make clear that it feeds the `{date}` placeholder in `filename_pattern` when pattern kind is template.
- Primary files: `frontend/src/components/pipeline/mnemonic-date-input.tsx`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `docs/build-specs/WORKLIST-RESOLUTIONS.md`.
- Evidence: `MnemonicDateInput` supports `dateTokenMode="filename"` and caption logic, but in the FileIngestion form the date picker is still visually separated from `filename_pattern` and `date_format`. The user has to infer that `loan_master_{date}.csv` + `date_value=PBD` + `date_format=yyyyMMdd` means "look for yesterday's business-date file rendered as `20260616`."
- Durable fix direction: group all filename-related controls into one construct. Show a sentence/preview like `loan_master_{date}.csv` with `{date}=PBD` and `yyyyMMdd` resolves to an example filename. Avoid exposing mnemonic/ISO/free-text as an isolated abstract date widget without explaining the filename substitution.

### LCT-026 — File Partitioning Must Be User-Configurable

- Repro: open the FileIngestion config UI and inspect `partition_by`.
- Observed: `partition_by` is read-only/derived.
- Expected: users should be able to set partition columns for the landed table; this is not solely a platform default.
- Primary files: `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`.
- Evidence: FileIngestion `partition_by` was seeded as `tier:"derived"`/`derivedFrom:"pipeline.storage"`, so `isDerivedParam` sends it to read-only rendering. Local metadata patch changed it to user-tier and removed `derivedFrom`.
- Temporary local workaround applied: updated local FileIngestion blueprint `params_schema` so `partition_by` is `tier:"user"` with description "Columns used to partition the landed table..."
- Durable fix direction: update canonical catalog/migrations and render `partition_by` with the existing multi-column picker where schema columns are available. Suggested defaults are fine; hard read-only platform ownership is not.

### LCT-027 — `Resolved by PULSE` Must Reveal The Resolved Value

- Repro: open a config panel row that displays `Resolved by PULSE`.
- Observed: the UI says `Resolved by PULSE` and may show `Derived from ...`, but does not show the resolved value.
- Expected: every derived/read-only value should show the actual value, its source, and a clear unresolved/error state if no value can be resolved.
- Primary files: `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `backend/src/main/java/com/pulse/pipeline/service/BlueprintInstanceConfigurationService.java`, `backend/src/main/java/com/pulse/pipeline/controller/CompositionController.java`.
- Evidence: `formatParamValue` returns the placeholder text for `undefined`, `null`, or empty strings (`configure-transform-dialog.tsx:150-153`). `DerivedParamInspector` then renders that text plus `Derived from {source}` and `Read-only` (`configure-transform-dialog.tsx:1098-1124`). There is no concrete resolved-value payload visible to the renderer.
- Root-cause hypothesis: the UI has param metadata (`tier`, `derivedFrom`) but not the output of the resolver that computes canonical fields/defaults. The resolved state exists in backend services for at least storage fields, but it is not exposed consistently as a view model for the config panel.
- Durable fix direction: add resolved config metadata to composition/config responses, for example `{name, value, source, editable, status}`. For unresolved values, render `Unable to resolve` with the missing source, not `Resolved by PULSE`.

### LCT-028 — Lake Format Must Follow Runtime Storage Authority, Not Delta Defaults

- Repro: inspect FileIngestion / table-producing blueprint storage fields in GCP mode.
- Observed: catalog/UI/defaults can still show or imply `delta`.
- Expected: PULSE runtime mode determines physical format: GCP uses BigQuery-managed Iceberg tables; DPC uses Hive-managed Parquet files on S3 storage.
- Primary files: `backend/src/main/java/com/pulse/runtime/service/RuntimeAuthorityService.java`, `backend/src/main/resources/db/migration/V97__blueprint_params_storage_backend_uplift.sql`, `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`, codegen/scaffold files that still assume Delta.
- Evidence: `RuntimeAuthorityService` has GCP `bronze/silver -> iceberg_bq_managed` and DPC `bronze/silver/gold -> parquet`. However V97 and V153 seed `lake_format.default="delta"` broadly, seed/demo data contains DPC `delta`, and frontend `legalLakeFormats` still returns `delta` for DPC and GCP non-gold.
- Temporary local workaround applied: updated test instance `01KVBWNWKKMPX2QA08R359DY4P` to `storage_backend=GCP`, `lake_layer=bronze`, `lake_format=iceberg_bq_managed`; updated local FileIngestion blueprint `lake_format.default` and description.
- Durable fix direction: make runtime authority the single source of truth for `lake_format`; update catalog defaults/options, frontend legal-format matrix, seed/demo compositions, codegen, and deploy validation. Delta should not appear as a default in this GCP/DPC operating model.

### LCT-029 — Config Save Must Preserve Dataset/Connector Binding

- Repro: bind a manually added FileIngestion instance to `Loan Master`, then edit/save visible FileIngestion config such as filename/pattern fields.
- Observed: binding params disappeared from the composition API, leaving only filename params; output schema returned to audit-only.
- Expected: editing visible params must preserve SOR/connector/dataset binding and derived output schema.
- Primary files: `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `backend/src/main/java/com/pulse/pipeline/service/CompositionService.java`, `backend/src/main/java/com/pulse/pipeline/controller/CompositionController.java`, `frontend/src/components/pipeline/add-source-dialog.tsx`.
- Evidence: before rebound, API returned no `dataset_names`, no `connector_name`, no `qualified_names`, and `columns=8`; after reapplying the hidden binding params, API returned `dataset_names=['Loan Master']`, connector name, and `columns=29`.
- Root-cause hypothesis: visible metadata-driven save path can overwrite `params` with the currently visible metadata draft and lose binding keys not in `params_schema`, especially if `currentParams` had already been malformed or incomplete due JSONB serialization/state issues.
- Temporary local workaround applied: rebound instance `01KVBWNWKKMPX2QA08R359DY4P` to SOR `MSP`, connector `01KVBVJ4CKWCRSSQDTNQQRM5E2`, dataset `01KVBVTNG3QZNGX0F5Z881VVTM`, and repatched schema to 29 columns.
- Durable fix direction: dataset/connector binding should be its own stable model or a protected params namespace. Config saves must merge/preserve non-visible binding keys, and tests should cover filename edits after source binding.

### LCT-030 — FileIngestion Needs A Left-Side Input Handle For This Workflow

- Repro: try to connect an upstream node into FileIngestion on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`.
- Observed: FileIngestion has no orange target handle on the left.
- Expected: user expects FileIngestion to expose an input port so it can receive upstream/source context wiring.
- Primary files: `backend/src/main/resources/db/migration/V7__blueprints_catalog.sql`, `docs/build-specs/SPEC-blueprint-catalog.md`, `frontend/src/components/pipeline/dag-node.tsx`, `backend/src/main/java/com/pulse/pipeline/service/CompositionService.java`.
- Evidence: local catalog had `input_ports=[]` for FileIngestion. `DagNode` renders left handles only from `inputPorts.map(...)` (`dag-node.tsx:217-250`), and `CompositionService.validatePortName` validates target ports against blueprint `input_ports`; without a catalog input port, both UI and backend reject/omit target wiring.
- Temporary local workaround applied: updated local FileIngestion blueprint `input_ports` to `[{"name":"source_input","description":"Optional upstream source/dataset context for file ingestion wiring"}]`.
- Durable fix direction: decide the canonical semantic port name and update blueprint migrations/specs. If FileIngestion remains a source-only node in some contexts, the UI should explicitly prevent or explain invalid source-to-source wiring. If this workflow requires source/context input, make the input port first-class and teach schema propagation/codegen what the port means.

### LCT-031 — Schema Badges Must Match Wiring State

- Repro: with no wirings on pipeline `01KVBVCFXGPB1T5TQGWTDR826E`, inspect FileIngestion after prior dataset-binding/schema workaround.
- Observed: FileIngestion showed `29 columns` despite no visible graph edge from the source node.
- Expected: if schema is derived from upstream wiring, it should not include upstream fields until the wire exists. If schema is derived from hidden dataset binding instead, the UI must label that source explicitly.
- Screenshot: `CleanShot 2026-06-17 at 19.15.55.png`.
- Primary files: `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, `backend/src/main/java/com/pulse/pipeline/service/CompositionService.java`, `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/dag-node.tsx`.
- Evidence: composition API had `wirings=[]`; FileIngestion had `cols=29` and hidden `params.dataset_names=['Loan Master']`; after local reset, API shows zero wirings and both nodes at 8 columns. The misleading badge came from persisted `output_schema`, not current visible graph connectivity.
- Temporary local workaround applied: reset FileIngestion `output_schema` to audit-only columns until the wire exists. Follow-up correction: this exposed that the source `SnapshotIngestion` node also had only audit columns, so its schema was restored from the `Loan Master` dataset to 21 source fields plus 8 audit fields while leaving FileIngestion audit-only.
- Durable fix direction: schema propagation should be deterministic from current graph plus explicit source bindings. When a node is unwired, stale propagated schema should be cleared/recomputed. Node badges should expose whether columns came from dataset binding, upstream wire, declared output schema, or manual override.

### LCT-032 — Graph Editor Needs A Compact Authoring Mode

- Repro: author/wire pipeline steps in the graphical editor for `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E` with side navigation and right config panel visible.
- Observed: user feels congested trying to draw the pipeline. The canvas is constrained, while each node is visually heavy and shows several always-on state badges.
- Expected: graph authoring should feel canvas-first. During wiring/composition, nodes should carry only the minimum state needed to identify and connect them; details should be available through selection, hover, or the inspector.
- Primary files: `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/app/pipelines/[pipelineId]/page.tsx`.
- Evidence: current nodes show the title, blueprint key, category pill, params count, column count, schema pill, DQ pill, handles, and status badges all at once. The page also reserves space for left navigation/outline and right-side inspector/chat panels, leaving a relatively small active drawing area.
- Durable fix direction: add a compact graph mode or make it the default authoring mode. Suggested behavior: compact nodes with title + type icon + port handles only; hover/selection reveals schema/params/DQ details; selected node details move to the right inspector; side panels are collapsible; `fitView` and focus-on-selection work reliably; optional minimap/search supports navigation for larger DAGs.

### LCT-033 — Wired Ingestion Schema Propagation Drops Business Fields

- Repro: wire `Ingest from MSP (S3-compatible Object Storage Connector)` / `SnapshotIngestion.snapshot_output` to `FileIngestion.source_input` on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`.
- Observed: source node collapsed to `8 cols` and FileIngestion showed `8 in` / `16 cols`; all columns were PULSE audit columns.
- Expected: Loan Master business fields should survive propagation. In this test, both the source output and FileIngestion output should show 29 columns: 21 Loan Master fields plus the canonical 8 audit fields, with no audit duplication.
- Screenshot: `CleanShot 2026-06-17 at 19.22.45.png`.
- Primary files: `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, `backend/src/main/java/com/pulse/pipeline/opengine/SchemaOpEngine.java`, `backend/src/main/java/com/pulse/pipeline/opengine/ops/AddAuditColumnsOp.java`, `backend/src/main/java/com/pulse/pipeline/opengine/Schema.java`, `backend/src/main/java/com/pulse/pipeline/model/SubPipelineInstance.java`, `backend/src/main/java/com/pulse/sor/model/Dataset.java`.
- Evidence before workaround: composition API returned one wiring `snapshot_output -> source_input`; `instance_port_schemas` had `SnapshotIngestion.snapshot_output=8`, `FileIngestion.source_input=8`, and `FileIngestion.raw_output=16`, all starting with `_pulse_ingested_at` and ending with `_pulse_dag_id`.
- Root-cause hypothesis: this is the same JSONB DTO/entity family as LCT-020/LCT-021, plus a schema-wrapper mismatch. `Dataset.schema_snapshot` stores top-level `fields`, while propagation/op-engine normalizes around top-level `columns`; when Java entity mapping does not expose `params.dataset_ids` / `schema_snapshot.fields` as lists, propagation falls back to empty source schema and emits audit-only output. The op `add-audit-columns` was also non-idempotent, so audit-only upstream became 16 audit columns downstream.
- Temporary local workaround applied: `SchemaPropagationService` now reads raw `params::text` and `schema_snapshot::text` with `JdbcTemplate`/Jackson for dataset resolution, normalizes `fields` to `columns`, and `AddAuditColumnsOp` skips audit descriptors whose names already exist. After `./gradlew compileJava`, DevTools reload, and `POST /schema/recompute`, API shows both nodes at 29 columns with exactly 8 audit columns.
- Durable fix direction: repair JSONB mapping centrally for params/schema DTOs, standardize schema wrappers (`columns` vs `fields`) at API/model boundaries, and make audit-column composition idempotent by contract. Add regression tests for source dataset -> SnapshotIngestion -> FileIngestion wiring.

### LCT-034 — SOR-Original Source Nodes Must Not Offer `+ Add DQ`

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, inspect `Ingest from MSP (S3-compatible Object Storage Connector)` / `SnapshotIngestion`.
- Observed: the node shows `+ Add DQ`.
- Expected: this is the original file/source node from the SOR and should be read-only for DQ authoring. Users should not be allowed to add data quality rules to the SOR-origin node itself.
- Screenshot: `CleanShot 2026-06-17 at 19.30.41.png`.
- Primary files: `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/components/pipeline/expectation-picker.tsx`, backend blueprint catalog/migrations that define blueprint role/source ownership.
- Evidence: `dag-view.tsx` computes `dqAllowed` from category only: `INGESTION`, `TRANSFORM`, and `MODELING` are allowed. `DagNode` renders `+ Add DQ` whenever `onDqClick` is present. Because `SnapshotIngestion` is category `INGESTION`, it receives the DQ authoring affordance even when it represents an original SOR/source-root read.
- Root-cause hypothesis: DQ authoring policy is using the coarse blueprint category as a proxy for "table-producing and user-owned output." That is too broad. Source-root/SOR-owned nodes are not the right authoring surface; DQ should attach to pipeline-owned materialized outputs or explicit DQ validation steps.
- Durable fix direction: introduce/derive a DQ-authorable capability from blueprint metadata or instance context, for example `supportsUserDqExpectations`, `compositionRole`, or a source-owned/read-only flag. Hide or disable `+ Add DQ` for original SOR/source nodes, and show a reason if needed. Add a regression that `SnapshotIngestion` source-root nodes do not receive `onDqClick`, while valid downstream pipeline-owned nodes still do.

### LCT-035 — Feature Request: DQ Editor Needs Schema-Aware Column Selection And AI Suggestions

- Repro: open DQ Expectations for `File Ingestion` on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, add `Column Not Null`.
- Observed: user must type `column_name` by hand. There is no column selector. There is also no AI action to suggest DQ rules from the dataset/schema.
- Expected: column parameters should use the existing column selector UI component, with options from the selected node's current output/input schema. Multi-column expectations should use a multi-select column picker. The dialog should also include an AI button such as `Suggest DQ rules` that invokes the LLM, uses the dataset schema/profile/context, and auto-populates proposed expectations for user review.
- Classification: feature request / UX enhancement, not a product bug.
- Screenshot: `CleanShot 2026-06-17 at 19.31.55.png`.
- Primary files: `frontend/src/components/pipeline/expectation-picker.tsx`, existing column selector component(s), `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dq-recommendations.tsx`, backend DQ recommendation/chat endpoints.
- Evidence: `expectation-picker.tsx` declares `const col` as `{type:"string", placeholder:"column_name"}` and `colList` as a comma-separated `string[]`; the param renderer maps all non-boolean params to `<Input>` (`expectation-picker.tsx:45-49`, `340-383`). There is no button or handler in this dialog for AI DQ recommendation generation.
- Root-cause hypothesis: the expectation catalog is schema-agnostic and the renderer has no access to the selected instance's columns. Existing AI DQ recommendation components are separate from this manual picker and are not integrated into the authoring flow.
- Durable fix direction: pass schema columns into `ExpectationPicker`, render column params with the shared selector, render `string[]` column-list params with multi-select, validate missing/renamed columns, and add an AI suggestion action. The LLM prompt needs product work: it should include column names/types/nullability/PII flags, dataset/SOR/business context, known rules from PULSE, and return structured Great Expectations-compatible candidates with severity/rationale/confidence. Clicking `Suggest DQ rules` should populate the configured list, not only show recommendations elsewhere.

### LCT-036 — Saved DQ Rules Must Reopen And Change Node Affordance

- Repro: on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, open DQ Expectations for FileIngestion, add/save a rule, close the dialog, then click the node's DQ action again.
- Observed: user cannot pull the saved rule back up, and the node still shows `+ Add DQ`.
- Expected: if one or more DQ rules are saved, the node should indicate that state, for example `DQ rules (1)` or `Edit DQ`, and clicking it should reopen the existing saved rules for review/edit.
- Primary files: `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/components/pipeline/expectation-picker.tsx`, `backend/src/main/java/com/pulse/pipeline/controller/DqController.java`, `backend/src/main/java/com/pulse/pipeline/service/DqExpectationService.java`, `backend/src/main/java/com/pulse/pipeline/model/SubPipelineInstance.java`.
- Evidence: composition API returned `dqExpectations=[{type:'ExpectColumnValuesToNotBeNull', ...}]` for `FileIngestion`, while `params.dq_expectations` was absent. `CompositionPanel` passed `currentExpectations={(dqInstance.params?.dq_expectations as DqExpectation[] | undefined) ?? []}` to `ExpectationPicker`, so it ignored the canonical `dqExpectations` column. `DagNode` rendered static `+ Add DQ` whenever `onDqClick` existed and had no count/state label.
- Additional backend issue: raw DB currently has `dq_expectations=[{"type":"ExpectColumnValuesToNotBeNull","kwargs":{},"severity":"warning"}]`, while the composition API displayed `kwargs` as `{empty:true,traversableAgain:true}`. This is both missing required-param validation and another JSONB mapping symptom.
- Temporary local workaround applied: `CompositionPanel` now passes `dqInstance.dqExpectations` first, falling back to legacy `params.dq_expectations`; `DagView` passes `dqExpectationCount`; `DagNode` labels the button `DQ rules (n)` and updates the title when rules exist. Touched-file lint passed with `npx eslint src/components/pipeline/dag-node.tsx src/components/pipeline/dag-view.tsx src/components/pipeline/composition-panel.tsx`.
- Durable fix direction: make `dqExpectations` the single read/write authority in all UI paths, remove legacy `params.dq_expectations` dependence or provide a migration shim, validate required expectation params before save, normalize/repair JSONB expectation serialization, and add regression tests that saved DQ rules reopen intact and the node affordance reflects saved count.

### LCT-037 — FileIngestion Needs A Visible Add/Drop Column Path

- Repro: select/configure `FileIngestion` on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E` and look for controls to add or drop columns.
- Observed: no visible option to add columns or drop columns.
- Expected: user needs an obvious way to shape the ingested file schema. This could be direct FileIngestion controls, or an explicit guided action to add the correct downstream transform/cleaning/derive/drop-columns step.
- Primary files: `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/add-transform-dialog.tsx` if present, `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, op-engine `AddColumnOp` / `DropColumnsOp`.
- Evidence: `V153__builder_op_lists_and_param_tiering.sql` defines FileIngestion as `read-source -> add-audit-columns -> write-sink(bronze)` with blueprint params for file/source/storage concerns, not add/drop column controls. The build specs and op engine already include `add-column` and `drop-columns`; BronzeToSilverCleaning exposes `drop_columns`, and `SchemaPropagationService` has universal `derived_columns` / `dropped_columns` addenda, but these are not surfaced for FileIngestion in the current UI.
- Root-cause hypothesis: the Builder has the low-level op vocabulary for schema shaping, but the FileIngestion authoring surface only exposes ingestion/file-location parameters. There is no product bridge from "I just ingested fields" to "now add/drop/select columns" in the graphical editor.
- Durable fix direction: make the product decision explicit. If FileIngestion is allowed to shape landed output, expose add/drop controls using column pickers and expression builders, and wire them to canonical params/op behavior. If ingestion must stay pure, add a prominent action such as `Add cleaning step`, `Drop columns`, or `Derive column` that creates/configures the appropriate downstream blueprint and wires it automatically. Include schema propagation/codegen tests for add/drop effects.

### LCT-038 — Ports-Schema Column Lists Look Truncated

- Repro: select `Bronze-to-Silver Cleaning`, open the right inspector `Ports-Schema` tab on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`.
- Observed: metrics show `29 Input` and `29 Output`, but the list visibly shows only the first five attributes (`loan_id`, `borrower_name`, `ssn`, `email`, `phone`).
- Expected: when the panel says 29 columns, the user should be able to clearly inspect all 29 attributes without wondering whether the data is missing.
- Screenshot: `CleanShot 2026-06-17 at 19.39.42@2x.png`.
- Primary file: `frontend/src/app/pipelines/[pipelineId]/page.tsx`.
- Evidence: API verification for `BronzeToSilverCleaning` shows `output len 29`, first fields include `loan_id`, `borrower_name`, `ssn`, `email`, `phone`, and last fields include PULSE audit columns. `ColumnList` renders `columns.map(...)`, but the wrapper is `max-h-36 overflow-y-auto`, creating a small nested scroll area where only a few rows are visible in the inspector.
- Root-cause hypothesis: nested scrolling and lack of an affordance/count in the list section makes a complete schema look truncated. The UX fails the user's expectation even though the data is present.
- Fix 2026-06-18: increased the shared `ColumnList` scroll window from `max-h-36` (9rem) to `max-h-[22.5rem]`, exactly 2.5x the previous height. Verification: `npm run lint -- 'src/app/pipelines/[pipelineId]/page.tsx'` passed.
- Durable fix direction: add section counts such as `Input Columns (29)`, make the list area larger or use the main inspector scroll instead of nested tiny scrolls, add a sticky/visible scrollbar or "show all" affordance, and consider schema search/filter/table view for wide schemas.

### LCT-039 — Bronze-to-Silver Edit Step Crashes In MultiColumnPicker

- Repro: click `Edit Step` on `Bronze-to-Silver Cleaning` in `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`.
- Observed: Next.js runtime overlay: `displayCols.map is not a function`.
- Expected: edit dialog opens and shows Bronze-to-Silver cleaning controls, including multi-column pickers for trim/drop/drop-null/dedup fields.
- Screenshot: `CleanShot 2026-06-17 at 19.40.43.png`.
- Primary files: `frontend/src/components/pipeline/column-picker.tsx`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `frontend/src/hooks/use-upstream-schema.ts`, upstream schema endpoint/service.
- Evidence: stack points to `MultiColumnPicker` mapping `displayCols`. Bronze-to-Silver blueprint params include `ui_construct:"column-picker (multi)"`; `ConfigureTransformDialog` passes `columns={upstreamSchema}`. API shows the instance output schema has 29 columns, so the issue is the shape of the upstream schema value reaching the picker, not missing schema data.
- Root-cause hypothesis: `upstreamSchema` can be a non-array object due to upstream-schema endpoint/JSONB mapping/normalization issues, and `MultiColumnPicker` trusted the prop type at runtime.
- Temporary local workaround applied: `ColumnPicker` and `MultiColumnPicker` now normalize `columns` with `Array.isArray(columns) ? columns : []`; malformed schema falls back to manual text input instead of crashing. Verification: `npx eslint src/components/pipeline/column-picker.tsx src/components/pipeline/configure-transform-dialog.tsx` passed.
- Durable fix direction: fix `useUpstreamSchema` and the backend upstream-schema response to always return a real `SchemaColumn[]`; keep defensive guards in UI controls; add tests for BronzeToSilverCleaning edit dialog with normal, empty, and malformed upstream schema payloads.

### LCT-040 — Browser Annotation Tool Is Not Persisting Comments

- Repro: use the in-app browser `Annotate` feature on `http://localhost:3000/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, select a UI element, type an annotation, and submit.
- Observed: annotation mode allows selecting arbitrary elements, including application menu/UI chrome, but after typing the annotation nothing is recorded or returned as usable evidence.
- Expected: submitted annotations should persist and appear in the transcript/tooling feed with target selector/path, screenshot marker, and comment text. If only page content is intended to be annotatable, the tool should constrain or explain target selection.
- Classification: live-test tooling issue, not a PULSE product bug.
- Evidence: user reported multiple attempted annotations where target screenshots appeared, but typed comment text either was gibberish/test text or did not result in a useful recorded annotation; the latest explicit report says "once I type the annotation nothing happens. The annotation is not recorded."
- Durable fix direction: fix the in-app browser annotation submit/persist path, add user feedback after save/failure, and ensure annotations surface reliably to Codex with exact target metadata and screenshot.

### LCT-045 — MongoDB Sink Metadata And Credential Reference Semantics Are Wrong

- Repro: on `/targets/01KVC126VJRGPDHQQM6DZCMX1Z`, click the Dev credential dot for `MongoDB Connector`; on `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`, use `Add Sink` and select target `CCODS` / `MongoDB Connector`.
- Observed: before local repair, MongoDB target sink was attached to a source connector definition, credential dialog implied platform/storage-backend derived credentials, and add-sink configuration routed MongoDB to `LakeWriter` with an `Output Path` placeholder for S3/lake paths.
- Expected: MongoDB target connector should use a destination connector definition and `CONNECTOR_SPECIFIC` credential strategy. Credential UI should collect Secret Manager lookup inputs: Secret Manager URI, optional regional-secret location, and credential secret reference/pointer. Add-sink should select a database-style writer surface, not a lake writer.
- Screenshot: `CleanShot 2026-06-17 at 20.13.36.png`.
- Primary files: `frontend/src/components/sor/add-connector-dialog.tsx`, `frontend/src/app/targets/[targetId]/page.tsx`, `frontend/src/components/sor/credential-dialog.tsx`, `frontend/src/components/pipeline/add-sink-dialog.tsx`, `backend/src/main/java/com/pulse/sor/controller/ConnectorCatalogController.java`, `backend/src/main/resources/db/migration/V99__pulse_native_connector_catalog.sql`.
- Evidence: DB query showed `connector_instances.id=01KVC12RQSWHQNRYYT8KR1BEWK` was bound to `01JCONN0SRC0MONGODB00001|SOURCE`; after local repair it is bound to `01JCONN0DST0MONGODB00001|DESTINATION|CONNECTOR_SPECIFIC`. The destination connector API exposes required fields `connection_string` and `database`; that internal key/description still says `MongoDB connection URI`, which is a deeper catalog mismatch with the desired credential-reference model.
- Temporary local workaround applied: `AddConnectorDialog` accepts `connectorType` and target detail passes `DESTINATION`; `ConnectorCatalogController` normalizes raw `connection_spec::text`; local DB repointed the existing connector instance to the Mongo destination definition; `AddSinkDialog` maps MongoDB to `DatabaseWriter` and labels the table target as `Target Collection`; `CredentialDialog` displays Mongo-specific Secret Manager fields as `Secret Manager URI`, `Location (optional for regional secrets)`, and `Credential Secret Reference`.
- Verification: `npm run lint -- src/components/sor/credential-dialog.tsx src/components/pipeline/add-sink-dialog.tsx src/app/targets/[targetId]/page.tsx src/components/sor/add-connector-dialog.tsx` passed. `./gradlew compileJava` passed. API verification shows target connector `MongoDB|DESTINATION|CONNECTOR_SPECIFIC` and connector spec fields `connection_string` + `database`.
- Durable fix direction: move source/target compatibility enforcement server-side so a target cannot attach a source connector definition. Replace add-sink name heuristics with connector capability metadata. Update Mongo connector catalog/spec to model a credential secret reference explicitly, rather than exposing a misleading raw `connection_string` concept. Add browser or component tests for target connector creation, credential dialog rendering, and add-sink selection for MongoDB.

### LCT-046 — DAG Selection Is Coupled To Right Inspector Visibility

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; double-click/select `File Ingestion` so the right inspector is visible; close the inspector; single-click a DAG node.
- Observed: before local patch, the inspector could immediately reopen or reopen from a normal node click.
- Expected: close is sticky until the user explicitly reopens the inspector. DAG single-click should select/focus only; DAG double-click should open the inspector. Outline click can keep opening the inspector because that supports the earlier outline-navigation requirement.
- Primary files: `frontend/src/app/pipelines/[pipelineId]/page.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-view.tsx`.
- Evidence: `PipelineContent` rendered `WorkspaceStepInspector` whenever `selectedWorkspaceInstance` was non-null. `CompositionPanel` only exposed `onInstanceClick`, so every DAG click fed the same selected state. `onCompositionLoaded` also auto-selected the first instance when the current selection was empty.
- Temporary local workaround applied: added `workspaceInspectorOpen` as independent state; closing the inspector sets only the open flag false; `DagView` now supports `onInstanceDoubleClick`; workspace `CompositionPanel` single-click selects and double-click calls `onSelectedInstanceOpen`; outline selection calls the explicit open path.
- Verification: `npm run lint -- src/components/pipeline/dag-view.tsx src/components/pipeline/composition-panel.tsx 'src/app/pipelines/[pipelineId]/page.tsx'` passed. In-app browser verification after reload: no inspector on load, single-click FileIngestion left `Config`/`Ports-Schema` absent, double-click FileIngestion made `Config`/`Ports-Schema` visible, close removed the inspector, and a subsequent single-click did not reopen it.
- Durable fix direction: add Playwright/component tests around selected-vs-open inspector behavior, keep read-only selection separate from editability, and complete LCT-013 by centering the selected graph node when selected from the outline.

### LCT-047 — Add Sink Credential Badge Is False-Negative For Dev

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; click `Add Sink`; select `Target / CCODS`; inspect the MongoDB connector card.
- Observed: the card shows a red `Credentials need attention for Dev` badge.
- Expected: Dev credentials were already configured for the target MongoDB connector, so the Add Sink picker should show a configured/usable Dev credential state or at least not warn falsely.
- Screenshot: `CleanShot 2026-06-17 at 20.51.40.png`.
- Primary files to inspect later: `frontend/src/components/pipeline/add-sink-dialog.tsx`, `frontend/src/app/targets/[targetId]/page.tsx`, target/SOR credential status DTOs/controllers, connector instance credential persistence tables, and the Mongo destination connector metadata touched in LCT-045.
- Root-cause hypothesis: there are two credential-status paths. The target detail page now renders status from one source, while Add Sink likely computes/checks status independently and is out of sync after the Mongo connector was repointed from source to destination metadata. Also check whether credential rows are environment-keyed as `DEV` vs `Dev`, or tied to connector-definition id rather than connector-instance id.
- Investigation path: query credential records for connector instance `01KVC12RQSWHQNRYYT8KR1BEWK`; inspect the Add Sink API payload for the same connector; compare with target detail payload. Confirm whether the warning is only visual or blocks `Continue`.
- Durable fix direction: centralize credential readiness computation server-side, return one normalized environment status contract to both target detail and Add Sink, and add a regression for MongoDB target sink with configured Dev Secret Manager reference.

### LCT-048 — Sink Write Modes Need Connector-Specific Semantics

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; `Add Sink`; select MongoDB target connector; inspect the configure step.
- Observed: MongoDB sink shows generic `Append`, `Overwrite`, and `Merge` buttons.
- Expected: write modes should be connector-specific and should use accurate product language. MongoDB is document/collection-oriented, so `merge` should not appear as a generic relational/table merge unless it maps to a concrete Mongo update/upsert mode with required key fields. SQL Server-style destructive replacement should be called `truncate and reload`, not `overwrite`, when that is what the generated job will do.
- Primary file: `frontend/src/components/pipeline/add-sink-dialog.tsx`.
- Evidence: local code hardcodes `{["append", "overwrite", "merge"].map(...)}` for all sink types in `AddSinkDialog`, regardless of `WarehouseWriter`, `LakeWriter`, `DatabaseWriter`, `StreamWriter`, or connector family.
- External reference: MongoDB Spark Connector docs say supported batch save modes are `append` and `overwrite`, and warn that `overwrite` drops/recreates the target collection by default. The same docs expose write `operationType` as `insert`, `replace`, or `update`, with `upsertDocument` controlling insertion when no match exists. `truncateMode` can be `DROP` or `TRUNCATE`.
- Root-cause hypothesis: the first-pass sink dialog used a generic warehouse/lake mental model for every destination connector, while blueprint migrations already distinguish `DatabaseWriter` options such as `append` and `upsert` in older catalog versions.
- Durable fix direction: introduce connector capability metadata for write dispositions. Suggested MongoDB surface: `Insert new documents`, `Replace/upsert documents by key`, `Update documents by key`, and explicit destructive `Drop and reload collection` or `Truncate and reload collection` only if supported/allowed. Suggested SQL/JDBC surface: `Append`, `Upsert by key`, `Truncate and reload`; reserve `Merge` for engines where generated SQL actually uses `MERGE`.

### LCT-049 — New Node Placement Causes Manual Drag-To-Connect

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; add a new step/node from the graphical editor.
- Observed: the newly created node appears in a second row, requiring the user to drag it up to the first row before connecting it to the existing pipeline.
- Expected: new nodes should appear close to the selected node, nearest dangling port, or end of the current lane so the next action is wiring/configuration rather than layout cleanup.
- Primary files to inspect later: `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-view.tsx`, backend composition add-instance endpoint/service if it stores ordering/position, and any auto-layout helper in `frontend/src/lib/auto-layout*`.
- Root-cause hypothesis: new instance placement is derived from a generic graph layout rather than from the user's current selection or intended insertion point. The layout likely wraps or layers nodes based on topological/order heuristics without knowing that the user is building a mostly linear pipeline.
- Durable fix direction: add `insert after selected` or `connect from selected` semantics, place the new node to the right of the selected/dangling source with collision-aware spacing, and run a compact horizontal auto-layout after add only when it will not disrupt manually arranged nodes.

### LCT-050 — Selected DAG Node Needs Obvious Feedback

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; single-click a DAG node after the LCT-046 double-click-to-open patch.
- Observed: from the tester's perspective, it was not clear whether the node had been selected, so it was not clear that the toolbar `Edit Step` action would edit that node.
- Expected: single-click should visibly select a DAG node without opening the inspector, and the edit action should make clear it applies to the selected step.
- Primary files: `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`.
- Evidence: `DagView` already set React Flow node `selected: selectedInstanceId === inst.id`, but `DagNode` did not consume the `selected` prop for a clear custom-node visual treatment. The toolbar button also used generic text `Edit Step`.
- Temporary local workaround applied: `DagNodeComponent` now destructures `selected` from `NodeProps` and renders a primary ring plus `Selected` badge. The toolbar action now says `Edit Selected Step` and carries a title with the selected step name.
- Verification: `npm run lint -- src/components/pipeline/dag-node.tsx src/components/pipeline/composition-panel.tsx` passed. Browser verification confirmed the selected marker appears on a DAG node after single-click and the inspector does not open.
- Durable fix direction: add an automated selection-state test; consider rendering the selected step name next to `Edit Selected Step` or in a compact toolbar chip for extra clarity on crowded canvases.

### LCT-051 — Wire Deletion Exists Technically But Is Not Discoverable

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; attempt to remove the connection from `Bronze-to-Silver Cleaning` to `Write to MongoDB Connector`.
- Observed: there is no visible delete control on the wire, no selected-wire action in the toolbar, and no obvious details panel/control for deleting the connection.
- Expected: users should be able to delete a wire from the graphical editor without knowing hidden ReactFlow keyboard behavior.
- Primary files: `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, backend composition wiring delete controller/service.
- Evidence: `CompositionPanel.handleUnwire` already calls `api.delete(/composition/wirings/{wiringId})`; `DagView` passes `onEdgesDelete={editable ? handleEdgesDelete : undefined}` into ReactFlow. This means deletion is probably only reachable via selecting an edge and pressing Delete/Backspace, but the UI provides no visible affordance or hint.
- Retest 2026-06-18: later fix report claimed LCT-051 was addressed by a custom `DeletableEdge` with an inline delete button. Current source contains that component, but live testing on `/pipelines/01KVCFX9KHVBBEMSTCKRHGDX3G` still left the tester unable to find/delete the wire. Treat prior "fixed" status as failed acceptance/audit miss unless a browser proof shows the visible delete affordance is reachable and works end-to-end.
- Root-cause hypothesis: implementation relied on ReactFlow's default edge deletion interaction and did not productize it for end users. This is especially painful in a dense pipeline canvas where selecting thin edges is already hard.
- Durable fix direction: add explicit edge selection state, render an edge toolbar or inline delete button near selected edges, and/or show a selected-wire inspector with source/target and `Delete wire`. Use a confirmation or undo-friendly toast for destructive deletion. Ensure deletion triggers schema recomputation/refresh and does not leave stale propagated schemas.

### LCT-052 — PIIMasking Enum Options And Required Validation Fail

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; add or edit `PII Masking & Tokenization`; inspect `masking_strategy` / hash algorithm controls; save with required fields empty.
- Observed: dropdown is empty/not populated, and the dialog allows save even though required fields are missing.
- Expected: masking strategy should offer valid strategies such as `hash`, `redact`, `tokenize`, and `encrypt`; when strategy is hash, hash algorithm should offer valid algorithms such as `sha256`, `sha512`, and potentially approved algorithms only. Required fields should block save with inline validation.
- Primary files: `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `backend/src/main/resources/db/migration/V155__blueprint_param_ui_construct_hints.sql`.
- Evidence: V153 seeds `PIIMasking.masking_strategy` as a required enum with options `["hash","redact","tokenize","encrypt"]` and `hash_algorithm` with options `["sha256","sha512","md5"]`. If the live UI shows no dropdown values, either the live DB metadata is missing/clobbered or frontend enum option normalization is failing for this field. The save behavior also indicates the config dialog does not enforce required metadata before `PUT /composition/instances/{id}/params`.
- Investigation path: query live `blueprints.params_schema` for `PIIMasking`; inspect the API response for that blueprint; then trace how `ConfigureTransformDialog` turns enum metadata into select items and how it validates required fields before save.
- Durable fix direction: restore/canonicalize enum options in catalog/API, enforce required fields client-side and server-side, and add a PIIMasking edit-dialog regression for empty required fields plus populated enum options. Also product-check the allowed hash algorithms; `md5` may be inappropriate for PII masking in regulated settings.

### LCT-053 — PIIMasking Does Not Flag Missing Masked Column After Upstream Drop

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; configure `Bronze-to-Silver Cleaning` to drop `ssn`; wire `Bronze-to-Silver Cleaning.cleaned_output` into `PII Masking & Tokenization.sensitive_data`; configure PIIMasking with `columns_to_mask=["ssn"]`; recompute/view the DAG and validation surfaces.
- Observed: PIIMasking remains `schemaStatus=clean`; its input and output schemas have 26 columns and do not contain `ssn`; `/api/v1/versions/01KVBVCFXH8E3GBAWN6T1YJ8BX/schema-conflicts?includeResolved=false` returns no open conflict rows.
- Expected: this should be a schema conflict. PIIMasking references `ssn` as a required operational input, but that column is absent from the upstream schema after Bronze-to-Silver drops it. The DAG node should show a `Schema conflict` chip and the PIIMasking right inspector `Validation` tab should list a `MISSING_COLUMN` conflict for `ssn`.
- Live data evidence: DB query showed PIIMasking instance `01KVC3RQFXPBXHVRQGTREJCQ9B` has `params->columns_to_mask = ["ssn"]` and no `params->pii_columns`; `instance_port_schemas` for both `sensitive_data` and `masked_output` omit `ssn`; `schema_conflicts` has no rows for version `01KVBVCFXH8E3GBAWN6T1YJ8BX`.
- Primary files: `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, `backend/src/main/resources/db/migration/V153__builder_op_lists_and_param_tiering.sql`, `frontend/src/components/pipeline/schema-conflict-panel.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/app/pipelines/[pipelineId]/page.tsx`.
- Root-cause hypothesis: stale param-name drift in schema propagation. `V153__builder_op_lists_and_param_tiering.sql` defines PIIMasking with `columns_to_mask`, but `SchemaPropagationService.collectMissingChecks(...)` still checks only legacy `pii_columns`. `maskSchema(...)` also documents/reads `pii_columns` and `mask_specs`, not `columns_to_mask`, so the masking resolver may silently pass through even when PIIMasking is configured from the current UI/catalog.
- Where conflicts should show: `SchemaConflictPanel` fetches `/api/v1/versions/{versionId}/schema-conflicts?includeResolved=false` and filters by instance. It is rendered in the pipeline right inspector `Validation` tab and in the transform edit dialog. DAG nodes render `Schema conflict` from `instance.schemaStatus`.
- Durable fix direction: update PIIMasking schema propagation to recognize current `columns_to_mask` plus legacy `pii_columns`, ideally through blueprint `schema_behavior` rather than hardcoded per-blueprint drift. Recompute should emit a `MISSING_COLUMN` conflict with details `{missing_column:"ssn", source_param:"columns_to_mask", port_name:"sensitive_data"}` and set the PIIMasking instance status to conflict. Add a regression test covering upstream drop of a masked column, and connect it with LCT-052 so invalid required PIIMasking configs are blocked before save.

### LCT-054 — Blueprint Step Name Is Suggested And Not Editable

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; add or edit a blueprint step; attempt to rename/edit the blueprint step display name.
- Observed: the UI supplies a suggested/generated name for the blueprint step and the tester cannot directly edit that name.
- Expected: users should be able to edit the step/display name so the DAG can use business-meaningful names instead of only generated blueprint-derived labels.
- Primary files to inspect later: `frontend/src/components/pipeline/configure-transform-dialog.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/add-step-dialog.tsx` if present, backend composition instance create/update endpoints, and `SubPipelineInstance` name/title persistence.
- Root-cause hypothesis: the step creation/edit flow treats the display name as a derived label from blueprint metadata or suggested-name logic, and the edit dialog only exposes blueprint params rather than editable instance metadata such as `name`.
- Durable fix direction: expose editable instance metadata separately from blueprint params, persist the name through the composition instance update API, validate uniqueness/readability where needed, and ensure renamed nodes update consistently in the outline, DAG card title, right inspector, package/codegen labels, and any run logs.

### LCT-055 — Editable Blueprints Are Missing Universal Add/Remove Column Operations

- Repro: open `/pipelines/01KVBVCFXGPB1T5TQGWTDR826E`; select/edit multiple non-read-only blueprint nodes and look for a general way to add a derived column or remove/drop columns from each blueprint.
- Observed: the current UI does not provide a consistent `Add Column` / `Remove Column` affordance on editable blueprints. During testing, FileIngestion had no add/drop path at all, and when looking for add-column behavior there was no expression builder surfaced. Some blueprint-specific params expose partial column behavior, such as Bronze-to-Silver `drop_columns`, but the general add/remove column capability from the previous version is missing.
- Expected: every user-editable blueprint that owns/transforms pipeline schema should expose common schema-shaping operations, except read-only/source-root connector nodes that represent the original file/SOR contract. `Add Column` should open the expression builder so the user can define the new column name, type, and expression. `Remove Column` / `Drop Column` should open a schema-aware column selector. These common operations should be available consistently, similar to how DQ is exposed as a common per-node affordance where valid.
- Primary files to inspect later: `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, `frontend/src/components/pipeline/configure-transform-dialog.tsx`, expression builder components such as `frontend/src/components/pipeline/expression-input.tsx` and `frontend/src/components/pipeline/simple-sql-builder.tsx`, blueprint catalog metadata/migrations for schema-shaping capabilities, `backend/src/main/java/com/pulse/pipeline/service/SchemaPropagationService.java`, op-engine `AddColumnOp` / `DropColumnsOp`, and codegen paths that consume derived/dropped column params.
- Root-cause hypothesis: the low-level op vocabulary and older UI had add/drop column concepts, but the new metadata-driven blueprint UI only renders params declared directly on each blueprint. Common schema-shaping ops (`add-column`, `drop-columns`, expression-derived columns) are not modeled as reusable per-blueprint capabilities, so they disappear unless a specific blueprint happens to declare a bespoke param.
- Retest 2026-06-18: bugfix report claimed V159/V160 fixed this by adding `derived_columns` / `dropped_columns` params and UI construct hints. Live browser on `/pipelines/01KVCFX9KHVBBEMSTCKRHGDX3G` still has no `+ Add Column` / `Remove Col` text in the DAG DOM. DB evidence: Flyway V159/V160 are successful, but active catalog rows use lowercase `status='active'`; the migrations filter with uppercase `status = 'ACTIVE'`, so the UPDATEs skipped `BronzeToSilverCleaning`, `PIIMasking`, `SchemaNormalization`, `GenericJoin`, etc. `Derive` has the params only because it already had native entries from V102. Treat prior fixed status as failed acceptance/audit miss.
- Fix 2026-06-18: added forward migration `V161__repair_schema_shaping_status_case.sql` rather than editing already-applied V159/V160. V161 replays the schema-shaping repair with `lower(status)='active'`, adds the correct UI construct hints inline, and patches existing derived/drop params. Verification: Flyway history shows V161 success; DB/API show `PIIMasking` has `derived_columns` with `derived-column-builder` and `dropped_columns` with `column-picker (multi)`; `BronzeToSilverCleaning` has native `drop_columns` plus `derived_columns` and no duplicate `dropped_columns`. Focused backend tests still pass.
- Class-level concern: this is not just a FileIngestion bug. Audit every editable blueprint category and decide which nodes are schema-owning and can safely add/drop columns, which are read-only source-root connector nodes, and which should expose add/drop through a downstream generated transform instead.
- Durable fix direction: introduce a common `schemaShaping` capability or operation rail for editable blueprint instances. Render `Add Column` and `Remove Column` affordances consistently for eligible nodes. Wire `Add Column` to the existing expression builder, persist derived-column params in a canonical shape, propagate schema changes, generate code, and show conflicts when downstream references removed columns. Add regression coverage across at least FileIngestion, BronzeToSilverCleaning, PIIMasking/downstream transform, and a read-only source-root node that must not show the controls.

### LCT-056 — Source Node Ignores Dataset Fields And Emits Audit-Only Schema

- Repro: on 2026-06-18, create a fresh UI pipeline `Loan Master Curation Pipeline 04:07:49` (`01KVCEN9P2Z6JPW65ACY8AWM26`), add source SOR `MSP`, select `S3-compatible Object Storage Connector`, select seeded dataset `Loan Master`, name the source step `Ingest from MSP`, and add it to the pipeline.
- Observed: the resulting `SnapshotIngestion` node shows `→ 8 cols` and the persisted instance output schema has 8 columns. It is bound to dataset `01KVBVTNG3QZNGX0F5Z881VVTM`, but only audit columns appear. The same failure was reproduced again on pipeline `01KVCF5P4W596PX6P3TA0MCY1N`, instance `01KVCF65S7A4C995P943XBB7BP`.
- Expected: source output should expose the 21 Loan Master fields plus 8 PULSE audit columns, for 29 output columns.
- Evidence: browser node text after add: `Ingest from MSP SnapshotIngestion ... → 8 cols ... audit · 8`. DB query: `sub_pipeline_instances` for pipeline `01KVCEN9P2Z6JPW65ACY8AWM26` shows instance `01KVCEV3WSQ69P6C3RY1ASJ9R1`, `blueprint_key=SnapshotIngestion`, `dataset_ids=["01KVBVTNG3QZNGX0F5Z881VVTM"]`, `jsonb_array_length(output_schema->'columns')=8`. Fresh DB query for pipeline `01KVCF5P4W596PX6P3TA0MCY1N` shows the same dataset id and `output_cols=8`. Dataset query shows `schema_snapshot.fields` length 21 and `schema_snapshot.columns` length 0.
- Root cause: `SnapshotIngestion` uses the V153 op-list path (`read-source -> add-audit-columns -> write-sink`). `SchemaPropagationService.resolveInputs` passes the dataset's raw `schemaSnapshot` as `primary`, and `isEmptySchema(primary)` correctly sees the `fields` array via `extractColumns`. Because it is not considered empty, `deriveBaseOutputSchema` does not replace it with `sourceRootSchema(inst)`. It then calls `schemaOpEngine.applyOpListAsMap(...)`, whose `Schema.fromMap(...)` only reads the `columns` key and ignores `fields`, so the op-engine starts from an empty source schema. `add-audit-columns` then appends the canonical 8 audit columns, producing audit-only output. Key files: `SchemaPropagationService.java` around the read-source op-list handling and `Schema.java` around `fromMap`.
- Investigation path: inspect `SchemaPropagationService.deriveBaseOutputSchema`, `sourceRootSchema`, `extractColumns`, and `Schema.fromMap`. Verify whether every op-engine entrypoint normalizes dataset snapshots from `{fields:[...]}` to canonical `{columns:[...]}` before converting to `Schema`.
- Durable fix direction: normalize dataset schema snapshots before op-engine conversion, preferably by centralizing the wrapper conversion so `{fields:[...]}` and `{columns:[...]}` both become canonical columns. Add a regression test that creates a `SnapshotIngestion` from a dataset whose snapshot uses `fields`, then asserts source output has source fields plus exactly 8 audit columns and not audit-only output. Also test `SchemaOpEngine.applyOpListAsMap` or `Schema.fromMap` with a `fields` wrapper if that boundary is intended to accept dataset snapshots directly.
- Fix applied locally 2026-06-18: `Schema.fromMap` now accepts both `columns` and dataset `fields` wrappers, and `SchemaPropagationService` reads dataset `schema_snapshot::text` through `JdbcTemplate` + Jackson before falling back to the repository JSONB mapping. Regression coverage added for `Schema.fromMap`, `SchemaOpEngine.applyOpListAsMap`, and `SchemaPropagationService` with a `SnapshotIngestion` dataset snapshot stored as `fields`.
- Verification: `./gradlew test --tests 'com.pulse.pipeline.opengine.ColumnModelTest' --tests 'com.pulse.pipeline.opengine.SchemaOpEngineTest' --tests 'com.pulse.pipeline.service.SchemaPropagationServiceTest' --tests 'com.pulse.pipeline.controller.SchemaPropagationControllerTest'` passed. After backend DevTools reload, re-saving source instance `01KVCF65S7A4C995P943XBB7BP` on pipeline `01KVCF5P4W596PX6P3TA0MCY1N` persisted 29 output columns: `loan_id` first, `as_of_date` at column 21, `_pulse_ingested_at` at column 22, and `_pulse_dag_id` at column 29. Composition API readback also returns 29 columns for the `SnapshotIngestion` instance.

### LCT-057 — Data Sink CCODS Appears Under Data Sources

- Repro: on 2026-06-18, open the local app during the recreate test and inspect the Data Sources/SOR source-selection surface. `CCODS` appears alongside actual source SORs such as `MSP`.
- Observed: `CCODS` is a data sink/target but is showing up under Data Sources.
- Expected: destination targets/sinks must be listed only under Data Sinks / target-selection flows. Data Sources / SOR source-pickers should list source systems only.
- Evidence: user reported while browser was on `/targets`: “CCODS is a data sink but showing up under data sources.” Earlier source-add dialog also showed both `CCODS` and `MSP` as selectable SORs for `Add Data Source`.
- Root-cause hypothesis: the UI or backend list used by source flows is pulling all domain-scoped systems/records without filtering by source-vs-destination role, or the seeded `CCODS` entity is persisted in the wrong registry/table/type. This is likely related to the earlier MongoDB target metadata issue where destination connectors could be treated like source/lake connectors.
- Investigation path: inspect the producer/source list API, target list API, seed migrations for `CCODS`, and source-add dialog data fetch. Verify connector/target records have a canonical role/type such as `SOURCE` vs `DESTINATION`, and ensure every Data Sources surface filters to source entities only.
- Durable fix direction: make source and sink registries/surfaces role-safe end to end: seed data, backend repositories/controllers, frontend list pages, and Add Source/Add Sink pickers. Add regression coverage that `CCODS` appears in Data Sinks and never appears in Data Sources/Add Source, while `MSP` appears in Data Sources/Add Source and not as a sink target.

### LCT-058 — Selected DAG Blueprint Cannot Be Deleted With Keyboard

- Repro: on 2026-06-18, open pipeline `/pipelines/01KVCFX9KHVBBEMSTCKRHGDX3G`, select a blueprint node in the DAG, and look for a way to delete it.
- Observed: there is no visible delete button/action for the selected blueprint node. User expectation is that selecting the node and pressing `Delete` should delete it.
- Expected: a selected DAG node should support keyboard deletion via `Delete` and likely `Backspace`, with a confirmation if the node has incoming/outgoing wires or downstream impact. The UI should also make node selection clear enough that the user knows which node will be deleted.
- Related issue: LCT-051 tracks missing wire/edge deletion affordance. This issue is specifically node/blueprint-instance deletion.
- Primary files to inspect later: `frontend/src/components/pipeline/dag-view.tsx`, `frontend/src/components/pipeline/composition-panel.tsx`, `frontend/src/components/pipeline/dag-node.tsx`, and `backend/src/main/java/com/pulse/pipeline/controller/CompositionController.java`.
- Root-cause hypothesis: backend deletion exists (`DELETE /api/v1/versions/{versionId}/composition/instances/{instanceId}`), but the React Flow selection/keyboard path either is disabled, not wired to `handleRemoveInstance`, or lacks a focused canvas key handler. The toolbar also lacks a discoverable delete action for the selected node.
- Fix 2026-06-18: `DagView` now accepts `onDeleteInstance` and registers a guarded `Delete`/`Backspace` key handler when an editable node is selected. The handler ignores text inputs, textareas, selects, and content-editable targets, then calls the existing `CompositionPanel.handleRemoveInstance` path. `CompositionPanel` clears the selected instance after successful deletion and passes the handler in both embedded and full-screen DAG modes, with the embedded handler disabled while the full-screen DAG is open to avoid double-firing a delete request. Verification: `npm run lint -- src/components/pipeline/dag-view.tsx src/components/pipeline/composition-panel.tsx` passed.
- Durable fix direction: wire selected-node keyboard deletion to the existing remove-instance API, support `Delete`/`Backspace`, block or confirm deletion when the node has wires, recompute schema after deletion, and add a visible `Delete selected step` action plus regression coverage for keyboard deletion.

## Observations

| Time | Area | Observation | Evidence / Notes |
|---|---|---|---|
| 2026-06-17 10:12 ET | Local startup | Postgres started healthy on `5432`; backend booted on `8080`; frontend served `http://localhost:3000`; backend `/actuator/health` initially returned `DOWN` until Redis was started, then returned `UP`. | `docker compose up -d postgres redis`; backend `./gradlew bootRun`; frontend `npm run dev`; `curl http://localhost:8080/actuator/health`; `curl -I http://localhost:3000` |
| 2026-06-17 10:31 ET | Seed data | Sales Daily Revenue composition data exists on active Rev #2: 4 instances and 3 wirings. | `GET /api/v1/versions/01JVER0SALES0REV0V100001/composition`; browser snapshot on Home Lending D&I shows "4 steps, 3 connections". |
| 2026-06-17 11:13 ET | Local startup | Latest build health checks are green: backend `/actuator/health` returns `UP`; frontend `HEAD /pipelines/01JPIPE0SALES0REVENUE0001` returns `200`; Flyway history has V153 and V154 successful; Sales Daily Revenue composition endpoint returns 4 instances and 3 wirings. | `curl http://localhost:8080/actuator/health`; `curl -I http://localhost:3000/pipelines/01JPIPE0SALES0REVENUE0001`; `docker compose exec -T postgres psql -U pulse -d pulse -Atc "select version, description, success from flyway_schema_history where version in ('153','154')"`; `GET /api/v1/versions/01JVER0SALES0REV0V100001/composition` with tenant/environment headers. |
| 2026-06-17 11:39 ET | Graph/staging verification | Automated graph and staging evidence is current, but live graph runtime was not reached without LLM config. | Backend graph test: `./gradlew test --tests 'com.pulse.chat.orchestration.GraphDriverTurnTest'` -> `BUILD SUCCESSFUL`. Frontend staging tests: `npm run test:unit -- src/lib/staging-reconcile.test.ts src/lib/auto-layout.test.ts` -> 2 files passed, 22 tests passed. Live SSE capture `/tmp/pulse-graph-smoke.sse` emitted `chunk`/`done` only; no preview plan row exists for session `01KVB3Q0XE3Y75JV51N4XT4F69`. |
| 2026-06-17 18:14 ET | Local restart after fixed build | Postgres and Redis were confirmed running. Backend session `11969` booted on port `8080`, applied Flyway V155 (`blueprint param ui construct hints`), and health returned `UP`. Frontend session `77121` is ready on `http://localhost:3000`. Transcript monitor session `99017` is polling Postgres every 1 second. | `docker compose up -d postgres redis`; backend boot log; `curl -s -f http://localhost:8080/actuator/health`; `curl -s -I http://localhost:3000/settings`; `docker compose exec -T postgres psql -U pulse -d pulse -Atc "select version, description, success from flyway_schema_history order by installed_rank desc limit 5"`; `./scripts/monitor-chat-transcript.sh`. |
| 2026-06-17 18:31 ET | Create-pipeline workaround executed | Created `Pipeline 1` for Unsecured Lending / Domain 1 by POSTing the missing derived `defaultStorageBackend: "GCP"` directly to the backend. New pipeline id `01KVBVCFXGPB1T5TQGWTDR826E`; active version `01KVBVCFXH8E3GBAWN6T1YJ8BX`. | `POST /api/v1/tenants/tenant-unsecured-lending/pipelines` with `name`, `domainName`, `domainId`, and `defaultStorageBackend`. |
| 2026-06-17 18:47 ET | Dataset recovery workaround executed | Existing draft dataset `Loan Master` (`01KVBVTNG3QZNGX0F5Z881VVTM`) was patched directly to unblock testing: status `SCHEMA_DEFINED`, classification `CONFIDENTIAL`, 21 loan-master fields, discovery method `manual_test_recovery`. | `UPDATE datasets SET schema_snapshot = ...::jsonb, schema_format='JSON_SCHEMA', classification='CONFIDENTIAL', status='SCHEMA_DEFINED', discovery_method='manual_test_recovery' WHERE id='01KVBVTNG3QZNGX0F5Z881VVTM'`; DB verification showed `field_count=21`. |
| 2026-06-17 18:54 ET | Temporary schema display hot patch | Applied a local backend-only workaround so dataset reads return schema fields as real arrays and the producer page can continue testing. This does not close LCT-020 as a durable product fix. | `DatasetController` response normalization via raw `schema_snapshot::text` + Jackson parse. Verification: `./gradlew compileJava` passed, backend restarted, `/actuator/health` returned `UP`, dataset API returned `fields_type=list` and `fields_count=21`. |
| 2026-06-17 18:57 ET | Temporary composition schema hot patch | Applied a local backend-only workaround so composition reads return `outputSchema.columns` as a real array and the newly created pipeline page can load again. This does not close LCT-021 as a durable product fix. | `CompositionController` response normalization via raw `output_schema::text` + Jackson parse. Verification: `./gradlew compileJava` passed, DevTools restarted backend, `/actuator/health` returned `UP`, composition API returned `columns_type=list` and `columns_count=8`, pipeline route returned `HTTP/1.1 200 OK`. |
| 2026-06-17 19:04 ET | Temporary FileIngestion binding workaround | Linked the existing FileIngestion step to `Loan Master` and patched its displayed output schema so pipeline testing can continue. This does not close LCT-022 as a durable product fix. | `PUT /api/v1/versions/01KVBVCFXH8E3GBAWN6T1YJ8BX/composition/instances/01KVBWNWKKMPX2QA08R359DY4P/params` with dataset/SOR/connector params; SQL patched output schema to source fields plus audit columns. Verification: composition API returns `dataset_names_type=list`, `dataset_ids_type=list`, and `columns_count=29`. |
| 2026-06-17 19:05 ET | Temporary FileIngestion config metadata patch | Patched local blueprint metadata so `pattern_kind` has options and `date_format` is editable for continued UI testing. This does not close LCT-023 or LCT-024 as durable product fixes. | SQL updated FileIngestion and FileArrivalSensor `params_schema`: `pattern_kind.options=["template","literal","glob","regex"]`; `date_format.tier="user"` and no `derivedFrom`. Verification query shows `filename_pattern`, `pattern_kind`, `date_value`, and `date_format` are all user-tier, and only `pattern_kind` carries enum options. |
| 2026-06-17 19:08 ET | Temporary partition metadata patch | Patched local FileIngestion metadata so `partition_by` is editable for continued UI testing. This does not close LCT-026 as a durable product fix. | SQL updated FileIngestion `params_schema`: `partition_by.tier="user"` and no `derivedFrom`. Verification query shows `partition_by|user|derivedFrom=false|string[]`. |
| 2026-06-17 19:10 ET | Temporary lake format storage-rule patch | Patched current FileIngestion instance and local FileIngestion blueprint metadata to use GCP BigQuery-managed Iceberg for continued testing. This does not close LCT-028 as a durable product fix. | SQL set instance `01KVBWNWKKMPX2QA08R359DY4P` to `lake_layer=bronze`, `lake_format=iceberg_bq_managed`; updated local FileIngestion `lake_format.default=iceberg_bq_managed`. Verification query shows `GCP|bronze|iceberg_bq_managed`. |
| 2026-06-17 19:12 ET | Temporary rebind after config clobber | Reapplied FileIngestion dataset/connector binding after config edit dropped hidden source params and schema regressed to audit-only. This does not close LCT-029 as a durable product fix. | `PUT /composition/instances/01KVBWNWKKMPX2QA08R359DY4P/params` restored SOR/connector/dataset params; SQL restored output schema to 21 source fields + 8 audit fields. Verification: `dataset_names=['Loan Master']`, connector name present, `lake_format=iceberg_bq_managed`, `columns=29`. |
| 2026-06-17 19:15 ET | Temporary FileIngestion input-port patch | Added a local FileIngestion input port so a left-side target handle appears after blueprint reload. This does not close LCT-030 as a durable product fix. | SQL set FileIngestion `input_ports` to `[{"name":"source_input","description":"Optional upstream source/dataset context for file ingestion wiring"}]`. Verification query shows the port; backend health `UP`; pipeline route returns `200`. |
| 2026-06-17 19:16 ET | Temporary unwired schema reset | Reset FileIngestion output schema to audit-only because it was showing dataset fields with no visible wiring. This does not close LCT-031 as a durable product fix. | SQL copied the 8 audit columns back to FileIngestion output schema. Verification: composition API shows `wirings=0`, FileIngestion `cols=8`, SnapshotIngestion `cols=8`, backend health `UP`. |
| 2026-06-17 19:18 ET | Source schema restored after workaround side effect | Restored the source `SnapshotIngestion` node to the Loan Master schema while keeping the unwired FileIngestion node audit-only. | SQL rebuilt source output schema as 21 dataset fields plus 8 audit fields. Verification: composition API shows `wirings=0`, `SnapshotIngestion cols=29 first=loan_id last=_pulse_dag_id`, `FileIngestion cols=8 first=_pulse_ingested_at last=_pulse_dag_id`. |
| 2026-06-17 19:27 ET | Temporary schema propagation hot patch | Patched local backend propagation so wired ingestion nodes preserve Loan Master fields and do not duplicate audit columns. This does not close LCT-033 as a durable product fix. | `SchemaPropagationService` raw-reads params/schema JSONB and normalizes `fields` to `columns`; `AddAuditColumnsOp` de-dupes audit names. Verification: `./gradlew compileJava` passed; backend health `UP`; `POST /schema/recompute` returned `processed=2, conflicts=0`; composition API and `instance_port_schemas` show `SnapshotIngestion.snapshot_output=29`, `FileIngestion.source_input=29`, `FileIngestion.raw_output=29`. |
| 2026-06-17 19:38 ET | Temporary DQ reopen/label hot patch | Patched local frontend so DQ dialog reopens from canonical `dqExpectations` and the node button shows configured-rule count. This does not close LCT-036 as a durable product fix. | Updated `CompositionPanel`, `DagView`, and `DagNode`; verification: `npx eslint src/components/pipeline/dag-node.tsx src/components/pipeline/dag-view.tsx src/components/pipeline/composition-panel.tsx` passed. Backend still needs JSONB payload and required-param validation fixes. |
| 2026-06-17 19:42 ET | Temporary column-picker crash guard | Patched local frontend column pickers so malformed/non-array schema input does not crash the Bronze-to-Silver edit dialog. This does not close LCT-039 as a durable product fix. | `ColumnPicker` and `MultiColumnPicker` now use `Array.isArray(columns) ? columns : []`; verification: `npx eslint src/components/pipeline/column-picker.tsx src/components/pipeline/configure-transform-dialog.tsx` passed. |
| 2026-06-17 19:59 ET | Temporary Bronze mapper UX hot patch | Patched local frontend so Trim Columns has `Select string columns`, Fill Null Map is badge-first like Trim Columns, `Drop Null Columns` is hidden, and empty JSONB placeholder keys are quarantined. This does not close LCT-042 or LCT-043 as durable product fixes. | Updated `ColumnPicker`, `RenameMapper`, and `ConfigureTransformDialog`; verification: `npm run lint -- src/components/pipeline/column-picker.tsx src/components/pipeline/rename-mapper.tsx src/components/pipeline/configure-transform-dialog.tsx` passed; `npm run test:unit -- src/components/pipeline/column-picker.test.tsx src/components/pipeline/rename-mapper.test.tsx` passed with 18 tests. Browser verification: no runtime overlay, `Select string columns` present, `Fill Null Map` present as a badge selector, `Drop Null Columns` absent, no `traversableAgain` leak, no duplicate-target error. |
| 2026-06-17 20:16 ET | Temporary credential dialog hot patch | Patched local frontend target/producers credential dialog so credential clicks no longer crash and fake `Empty` / `TraversableAgain` fields are suppressed. This does not close LCT-044 as a durable product fix. | Updated `CredentialDialog`; verification: focused lint passed and browser verification on `/targets/01KVC126VJRGPDHQQM6DZCMX1Z` Dev credential button showed no runtime overlay and no placeholder fields. Follow-on testing found the platform-resolved message was wrong for MongoDB; see LCT-045. |
| 2026-06-17 20:24 ET | Temporary MongoDB target metadata hot patch | Patched local target connector flow so MongoDB target credentials and sink configuration use destination/database semantics instead of source/lake/storage semantics. This does not close LCT-045 as a durable product fix. | Existing connector instance `01KVC12RQSWHQNRYYT8KR1BEWK` was repointed to `01JCONN0DST0MONGODB00001`; `AddConnectorDialog` target usage now filters `DESTINATION`; `CredentialDialog` now shows `Secret Manager URI`, `Location (optional for regional secrets)`, and `Credential Secret Reference`, with no secret value/reference dropdown; `AddSinkDialog` maps MongoDB to `DatabaseWriter` and `Target Collection`; lint and backend compile passed. Browser verification confirmed the final labels/examples and no runtime overlay. |
| 2026-06-17 20:31 ET | Credential status affordance label patch | Patched connector rows so environment credential status controls are visibly labeled `Credentials` instead of appearing as unexplained dots. This does not close LCT-045 as a durable product fix. | Updated target and producer connector rows to render each env as a labeled pill with status dot + env abbreviation under a `Credentials` label. Verification: `npm run lint -- src/app/targets/[targetId]/page.tsx src/app/producers/[sorId]/page.tsx` passed. |
| 2026-06-17 20:50 ET | Temporary DAG inspector interaction patch | Patched local frontend so the right step inspector no longer reopens from ordinary DAG selection after the user closes it. This does not close LCT-046 as a durable product fix. | `workspaceInspectorOpen` now gates right inspector rendering; DAG nodes have separate click and double-click callbacks. Verification: focused lint passed; browser check confirmed single-click keeps inspector closed and double-click opens it. |
| 2026-06-17 20:52 ET | Add Sink false credential warning logged | Logged a deferred bug where Add Data Sink says MongoDB Dev credentials need attention even though Dev credentials were already configured. No app changes made per user request. | See LCT-047. Screenshot: `CleanShot 2026-06-17 at 20.51.40.png`. |
| 2026-06-17 20:55 ET | Add Sink write-mode research logged | Logged connector-specific write-mode issue for MongoDB and SQL/JDBC terminology. No app changes made. | See LCT-048. Sources checked: MongoDB Spark Connector batch write and batch write configuration docs; MongoDB `bulkWrite` and `deleteMany` docs. |
| 2026-06-17 20:55 ET | New-node placement issue logged | Logged deferred UX issue where newly created DAG nodes appear on a second row and require manual dragging before wiring. No app changes made. | See LCT-049. |
| 2026-06-17 20:58 ET | Temporary DAG selected-state affordance patch | Patched local frontend so single-clicked DAG nodes show an obvious selected state and the toolbar says `Edit Selected Step`. This does not close LCT-050 as a durable product fix. | Updated `DagNode` selected rendering and `CompositionPanel` edit label. Verification: focused lint passed and browser check showed a visible `Selected` marker without opening the inspector. |
| 2026-06-17 20:59 ET | Wire deletion affordance issue logged | Logged deferred UX/product issue where the DAG has no discoverable way to delete a wire, even though the delete API/hook exists. No app changes made. | See LCT-051. |
| 2026-06-17 21:00 ET | PII masking required enum/validation issue logged | Logged deferred bug where PIIMasking strategy/hash dropdowns are empty and required fields do not block save. No app changes made. | See LCT-052. |
| 2026-06-17 21:20 ET | Bug-fix handoff prepared and temporary app-code patches reverted | Created comprehensive bug-fix report and agent prompt, then reverted all tracked app-code hot patches from the live test session. These temporary patches should not be treated as durable fixes. | Artifacts: `docs/evidence/live-chat-bugfix-report-2026-06-17.md` and `docs/evidence/live-chat-bugfix-agent-prompt-2026-06-17.md`. Local Postgres test data/DB patches were intentionally not reset because they contain live test objects and evidence state; the fixing agent should start from clean seeded DB or document DB state. Worktree still has unrelated `.claude/scheduled_tasks.lock` deletion, `.codex/`, and transcript-monitor script untracked. |
